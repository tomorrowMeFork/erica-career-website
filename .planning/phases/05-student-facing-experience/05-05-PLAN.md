---
phase: 05-student-facing-experience
plan: 05
type: execute
wave: 4
depends_on: [05-03, 05-04]
files_modified:
  - components/dashboard/student-dashboard.tsx
  - app/globals.css
  - tests/phase5-web-smoke.spec.ts
  - scripts/verify-phase5-ui.ts
  - scripts/verify-phase5-ui.test.ts
  - package.json
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]
must_haves:
  truths:
    - "Mobile and desktop dashboard flows remain usable without losing citation context."
    - "Chat, listings, citations, and preferences use Korean-first copy and accessible focus/touch behavior."
    - "Phase 5 QA catches missing source metadata, unsafe links, missing deadline labels, prohibited scope, and design-token drift."
  artifacts:
    - path: "tests/phase5-web-smoke.spec.ts"
      provides: "Playwright mobile/desktop smoke for chat dashboard, citations, listings, preferences"
    - path: "scripts/verify-phase5-ui.ts"
      provides: "Static Phase 5 UI invariant checks"
    - path: "scripts/verify-phase5-ui.test.ts"
      provides: "Deterministic tests for UI verifier"
    - path: "components/dashboard/student-dashboard.tsx"
      provides: "Integrated responsive dashboard composition"
  key_links:
    - from: "tests/phase5-web-smoke.spec.ts"
      to: "app/page.tsx"
      via: "browser smoke against Next app"
      pattern: "page.goto"
    - from: "scripts/verify-phase5-ui.ts"
      to: "components/**/*, app/globals.css"
      via: "source inspection invariants"
      pattern: "모집중|마감됨|마감일 확인 필요"
---

<objective>
Integrate dashboard panels and add Phase 5 responsive/accessibility/static QA gates.

Purpose: Phase 5 is complete only when chat, citations, listings, preferences, and visual system work together on mobile and desktop without adding Phase 6 release scope.
Output: Responsive integrated dashboard, Playwright smoke, static UI verifier, npm verification script, and final build/typecheck/test gate covering UX-01 through UX-05.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-student-facing-experience/05-CONTEXT.md
@.planning/phases/05-student-facing-experience/05-UI-SPEC.md
@components/dashboard/student-dashboard.tsx
@components/chat/chat-composer.tsx
@components/citations/source-inspection-rail.tsx
@components/citations/mobile-source-sheet.tsx
@components/listings/listing-panel.tsx
@components/preferences/preference-panel.tsx
@app/globals.css

<interfaces>
Responsive breakpoints: mobile `<768px` single-column with source/preferences/listings bottom drawer/sheet and sticky composer; tablet `768px–1023px` collapsible panels above/below chat; desktop `1024px–1359px` three-zone layout with left panel `280px`, central chat flexible, right source rail `360px`; wide `>=1360px` max container `1440px`, right rail `400px`. Accessibility: focus ring `2px solid accent` plus offset; minimum touch target `44px`; Esc closes sheet/settings/dialog; source sheet close button first; source links `noopener noreferrer`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate responsive dashboard panels, wire API helpers, and accessibility behavior</name>
  <files>components/dashboard/student-dashboard.tsx, app/globals.css</files>
  <read_first>components/dashboard/student-dashboard.tsx, app/globals.css, components/chat/chat-composer.tsx, components/citations/source-inspection-rail.tsx, components/citations/mobile-source-sheet.tsx, components/listings/listing-panel.tsx, components/preferences/preference-panel.tsx, .planning/phases/05-student-facing-experience/05-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `StudentDashboard` calls `getOrCreateSessionKey` on mount and passes the returned session key to `sendChatMessage`, `fetchRecommendations`, `readPreferences`, `savePreferences`, `updatePreferences`, and `clearPreferences`.
    - Test 2: after a successful preference save or update via `savePreferences`/`updatePreferences`, the dashboard calls `fetchRecommendations` with the updated session key and refreshes listing panel items.
    - Test 3: after `clearPreferences`, the dashboard calls `fetchRecommendations` with no profile and refreshes listing panel items in no-preference mode.
    - Test 4: on mobile `<768px`, the dashboard renders chat primary; panel tab/buttons open listings, preferences, and source inspection as bottom drawers/sheets; each drawer closes on Escape and restores focus to the invoking control.
    - Test 5: on desktop `>=1024px`, the dashboard uses CSS grid with left panel `280px`, central chat `minmax(0, 1fr)`, and source rail `360px`; at `>=1360px` container caps at `1440px` and source rail expands to `400px`.
    - Test 6: every interactive control has at least `44px` hit target; keyboard tab order starts header, then panel controls, then chat actions, then composer, then side panels.
    - Test 7: clear chat history control shows confirmation copy `현재 브라우저 세션의 대화 기록을 지울까요? 서버에는 저장되지 않습니다.` and only clears client state.
  </behavior>
  <action>Compose chat, source inspection, listings, and preferences into the single dashboard per D-02. Wire all API helpers into integrated dashboard state: call `getOrCreateSessionKey` on mount and distribute the key to `sendChatMessage`, `fetchRecommendations`, `readPreferences`, `savePreferences`, `updatePreferences`, and `clearPreferences`. After each successful preference save/update, call `fetchRecommendations` with the current session key to refresh listing panel items. After `clearPreferences`, call `fetchRecommendations` with no profile to return to no-preference mode. Feed `readPreferences` result into `PreferencePanel` on mount. On mobile `<768px`, render chat primary with panel tab/buttons opening listings/preferences/source inspection as bottom drawers/sheets and sticky composer with safe-area padding. On tablet `768px–1023px`, render collapsible panels above/below chat. On desktop `>=1024px`, use CSS grid with optional left panel width `280px`, central chat `minmax(0, 1fr)`, and source rail `360px`; at `>=1360px`, cap container at `1440px` and allow source rail `400px`. Ensure keyboard tab order starts header/panel controls/chat actions/composer/side panels, Esc closes transient panels, focus returns to invoking control, and every interactive control has at least `44px` hit target. Add clear chat history control using the exact confirmation copy `현재 브라우저 세션의 대화 기록을 지울까요? 서버에는 저장되지 않습니다.` and only clear client state. Do not add Phase 6 release checklist, streaming, server-side chat-history persistence, SSO, saved jobs, reminders, resume/application tools, or advanced listing search.</action>
  <verify>
    <automated>npm run test:ui</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -n '280px\|360px\|400px\|1440px\|768px\|1024px\|1360px\|safe-area-inset-bottom\|44px' app/globals.css components/dashboard/student-dashboard.tsx` returns matches. Grep: `grep -n '현재 브라우저 세션의 대화 기록을 지울까요\|서버에는 저장되지 않습니다\|Escape\|focus' components/dashboard/student-dashboard.tsx components/citations/mobile-source-sheet.tsx` returns matches. Grep: `grep -n 'getOrCreateSessionKey\|fetchRecommendations\|readPreferences\|savePreferences\|updatePreferences\|clearPreferences' components/dashboard/student-dashboard.tsx` returns matches. Grep excluding comments: `grep -R -v '^#' components app | grep -c 'streaming\|EventSource\|server-side chat-history\|saved jobs\|reminders\|resume\|SSO'` outputs `0`.</acceptance_criteria>
  <done>The dashboard works as one responsive product surface with mobile/drawer and desktop/rail behavior, accessible focus/touch controls, and client-session-only history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 5 web smoke and static UI invariant gates</name>
  <files>tests/phase5-web-smoke.spec.ts, scripts/verify-phase5-ui.ts, scripts/verify-phase5-ui.test.ts, package.json</files>
  <read_first>tests/phase5-web-smoke.spec.ts, scripts/verify-phase5-ui.ts, scripts/verify-phase5-ui.test.ts, package.json, .planning/phases/05-student-facing-experience/05-UI-SPEC.md, .planning/phases/05-student-facing-experience/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: static verifier fails if required Korean strings, deadline labels, source link safety, design tokens, or `html lang="ko"` are missing.
    - Test 2: static verifier fails if prohibited Phase 5 scope strings appear in app/components/lib source: streaming, server-side chat history, SSO, saved jobs, reminders, resume, application submission, production crawling.
    - Test 3: Playwright smoke covers mobile width 390 and desktop width 1280 for dashboard load, empty chat copy, composer, listing panel/filter pills, preference panel, and source inspection shell behavior using mocked network where needed.
  </behavior>
  <action>Add `scripts/verify-phase5-ui.ts` as a deterministic source-inspection CLI that checks: `app/layout.tsx` has `lang="ko"`; `app/globals.css` contains UI-SPEC OKLCH tokens, font stack, focus ring, responsive widths; components contain Korean copy `질문 보내기`, `출처 확인하기`, `공식 페이지 열기`, `무엇을 도와드릴까요?`, `모집중`, `마감됨`, `마감일 확인 필요`, `현재 세션에만 저장`; source links use `noopener noreferrer`; no prohibited scope tokens appear outside planning docs/tests. Add `scripts/verify-phase5-ui.test.ts` with temporary fixture directories to prove pass/fail behavior. Add package script `verify:phase5-ui: tsx scripts/verify-phase5-ui.ts`. Add `tests/phase5-web-smoke.spec.ts` using `@playwright/test`; it may mock `/api/chat`, `/api/recommendations`, and `/api/preferences` responses to avoid live provider/env, but must still exercise UI rendering, mobile/desktop viewport behavior, citation open/close, listing filters, and preference drawer/settings. Do not create full Phase 6 evaluation suite or release readiness checklist.</action>
  <verify>
    <automated>npm test -- scripts/verify-phase5-ui.test.ts</automated>
    <automated>npm run verify:phase5-ui</automated>
    <automated>npm run build:web</automated>
    <automated>npm run qa:web</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -n 'verify:phase5-ui' package.json` returns a match. Grep: `grep -n '질문 보내기\|공식 페이지 열기\|모집중\|마감됨\|마감일 확인 필요\|noopener noreferrer\|lang="ko"\|streaming\|server-side chat history' scripts/verify-phase5-ui.ts` returns matches. Grep: `grep -n '390\|1280\|page.route\|/api/chat\|/api/recommendations\|/api/preferences\|출처' tests/phase5-web-smoke.spec.ts` returns matches.</acceptance_criteria>
  <done>Phase 5 has repeatable UI QA gates for responsive behavior, Korean copy, citations, source-link safety, status labels, and scope control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport/user input→responsive UI | Different viewport and keyboard paths exercise the same source/citation data. |
| Static verifier→source tree | QA script reads source files and enforces Phase 5 invariants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | D | Mobile drawers/focus | mitigate | Smoke checks mobile open/close paths; components implement Esc/close/focus restoration. |
| T-05-15 | T | Source links/status labels | mitigate | Static verifier requires `noopener noreferrer`, Korean deadline labels, source metadata strings, and blocks missing labels. |
| T-05-16 | I | Prohibited persistence/scope | mitigate | Static verifier blocks server chat-history persistence, SSO, saved jobs, reminders, resume/application/crawling strings in app source. |
</threat_model>

<verification>
Run `npm test -- scripts/verify-phase5-ui.test.ts`, `npm run verify:phase5-ui`, `npm run typecheck`, `npm test`, `npm run test:ui`, `npm run build:web`, and `npm run qa:web`.
</verification>

<success_criteria>
All Phase 5 UX requirements are covered by integrated responsive UI behavior and repeatable QA gates, without adding Phase 6 or v2 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-facing-experience/05-05-SUMMARY.md`.
</output>
