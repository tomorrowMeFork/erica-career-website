---
phase: 05-student-facing-experience
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - app/api/chat/route.ts
  - app/api/recommendations/route.ts
  - app/api/preferences/route.ts
  - lib/service-container.ts
  - lib/api-client.ts
  - lib/session-key.ts
  - lib/deadline-labels.ts
  - lib/api-client.test.ts
  - lib/deadline-labels.test.ts
  - app/api/chat/route.test.ts
  - app/api/recommendations/route.test.ts
  - app/api/preferences/route.test.ts
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Browser helpers can submit complete chat requests and validate ChatResponse before rendering."
    - "Browser helpers can load recommended/latest listings and validate RecommendationResponse before rendering."
    - "Preference set/update/clear flows use existing PreferenceService contracts and session-first keys."
    - "Deadline status values map to Korean semantic labels without inference."
  artifacts:
    - path: "app/api/chat/route.ts"
      provides: "POST /api/chat complete-response service adapter"
    - path: "app/api/recommendations/route.ts"
      provides: "POST/GET recommendation service adapter"
    - path: "app/api/preferences/route.ts"
      provides: "GET/POST/PATCH/DELETE preference lifecycle adapter"
    - path: "lib/api-client.ts"
      provides: "Client fetch helpers with Zod validation and Korean error conversion"
    - path: "lib/deadline-labels.ts"
      provides: "active/expired/unknown and match/refusal label mappings"
  key_links:
    - from: "app/api/chat/route.ts"
      to: "src/chat/chat-service.ts"
      via: "getChatService().ask"
      pattern: "getChatService\(\).*ask"
    - from: "app/api/recommendations/route.ts"
      to: "src/recommendations/recommendation-service.ts"
      via: "getRecommendationService().recommend"
      pattern: "getRecommendationService\(\).*recommend"
    - from: "app/api/preferences/route.ts"
      to: "src/personalization/preference-service.ts"
      via: "getPreferenceService lifecycle methods"
      pattern: "setPreferences|updatePreferences|clearPreferences|readState"
---

<objective>
Add thin Next.js Node route handlers and client/server helper modules over the existing chat, recommendation, and preference services.

Purpose: Phase 5 UI must use real Phase 3/4 contracts through thin adapters per D-03, not a separate backend or mock-only UI.
Output: Validated `/api/chat`, `/api/recommendations`, `/api/preferences`, service factories, browser fetch helpers, session key helper, deadline/status label helper, and tests.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-student-facing-experience/05-CONTEXT.md
@.planning/phases/05-student-facing-experience/05-RESEARCH.md
@.planning/phases/05-student-facing-experience/05-PATTERNS.md
@.planning/phases/04-personalization-and-recommendations/04-04-SUMMARY.md
@.planning/phases/03-source-grounded-chat-mvp/03-07-SUMMARY.md
@src/chat/chat-contract.ts
@src/chat/chat-service.ts
@src/recommendations/recommendation-contract.ts
@src/recommendations/recommendation-service.ts
@src/personalization/preference-contract.ts
@src/personalization/preference-service.ts
@src/personalization/preference-store.ts
@src/ingestion/normalized-record.ts

<interfaces>
Route contracts: `/api/chat` POST parses `ChatRequestSchema` and returns `ChatResponseSchema`; `/api/recommendations` POST parses `RecommendationRequestSchema` and returns `RecommendationResponseSchema`; `/api/preferences` requires `session_key` and supports GET read, POST set, PATCH update, DELETE clear over `PreferenceService`. All handlers export `runtime = "nodejs"` and `dynamic = "force-dynamic"`. Deadline labels: `active -> 모집중`, `expired -> 마감됨`, `unknown -> 마감일 확인 필요`. Match labels: `personalized_match -> 맞춤 추천`, `partial_match -> 일부 조건 일치`, `general_recommendation -> 일반 안내`. Refusal labels: `hard_refuse -> 답변 근거 부족`, `soft_hedge -> 확인 필요`, `normal_answer -> 출처 기반 답변`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build server-only service factories and Node route adapters</name>
  <files>lib/service-container.ts, app/api/chat/route.ts, app/api/recommendations/route.ts, app/api/preferences/route.ts, app/api/chat/route.test.ts, app/api/recommendations/route.test.ts, app/api/preferences/route.test.ts</files>
  <read_first>lib/service-container.ts, app/api/chat/route.ts, app/api/recommendations/route.ts, app/api/preferences/route.ts, src/chat/chat-contract.ts, src/chat/chat-service.ts, src/recommendations/recommendation-contract.ts, src/recommendations/recommendation-service.ts, src/personalization/preference-contract.ts, src/personalization/preference-service.ts, src/personalization/preference-store.ts, .planning/phases/05-student-facing-experience/05-PATTERNS.md</read_first>
  <behavior>
    - Test 1: `/api/chat` accepts `{ query: "채용 공고 알려줘", top_k: 5 }`, calls a mocked `ChatService.ask`, and returns schema-valid complete JSON without streaming.
    - Test 2: `/api/chat` setup/provider errors return controlled 503 JSON with Korean copy `요청을 처리하지 못했어요. 잠시 후 다시 시도하거나 공식 출처 페이지에서 직접 확인해 주세요.` and no API key or raw stack trace.
    - Test 3: `/api/recommendations` returns schema-valid recommendations preserving citations, `deadline_status`, match strength, privacy metadata, and service-provided ordering.
    - Test 4: `/api/preferences` GET/POST/PATCH/DELETE validates `session_key`, required `major`/`target_role`, session-first storage, update, and clear semantics.
  </behavior>
  <action>Create tests first with deterministic mocks and injected service factories. Implement `lib/service-container.ts` with exported `getChatService()`, `getRecommendationService()`, `getPreferenceService()`, and test-only/injection-safe reset or override functions if needed. Factories should reuse `loadKnowledgeBaseChunks()`, `Bm25Retriever`, `ChatService`, `RecommendationService`, and `PreferenceService(new InMemoryPreferenceStore())`; catch provider env construction failures at route boundary. Implement App Router handlers with `export const runtime = "nodejs"`, `export const dynamic = "force-dynamic"`, Zod request parsing, service call, response schema parsing, and safe JSON errors. Do not implement a separate Express/Fastify server, streaming, server chat-history persistence, advanced listing search, SSO, crawling, or mocked fake answers. This implements D-03, D-04, D-06, D-10, and D-14.</action>
  <verify>
    <automated>npm test -- app/api/chat/route.test.ts app/api/recommendations/route.test.ts app/api/preferences/route.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -R -n 'runtime = "nodejs"\|dynamic = "force-dynamic"' app/api` shows all three route files. Grep: `grep -R -n 'ChatRequestSchema.parse\|ChatResponseSchema.parse\|RecommendationRequestSchema.parse\|RecommendationResponseSchema.parse\|PreferenceProfileSchema\|PreferenceStateSchema' app/api lib/service-container.ts` returns matches. Grep excluding comments: `grep -R -v '^#' app/api lib/service-container.ts | grep -c 'EventSource\|ReadableStream\|server-side chat history\|apply to jobs\|SSO'` outputs `0`.</acceptance_criteria>
  <done>API routes are thin, complete-response, schema-validated adapters over existing services with deterministic route tests and safe Korean error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add browser API helpers, session keys, and semantic labels</name>
  <files>lib/api-client.ts, lib/session-key.ts, lib/deadline-labels.ts, lib/api-client.test.ts, lib/deadline-labels.test.ts</files>
  <read_first>lib/api-client.ts, lib/session-key.ts, lib/deadline-labels.ts, src/chat/chat-contract.ts, src/recommendations/recommendation-contract.ts, src/personalization/preference-contract.ts, src/ingestion/normalized-record.ts, .planning/phases/05-student-facing-experience/05-UI-SPEC.md, .planning/phases/05-student-facing-experience/05-PATTERNS.md</read_first>
  <behavior>
    - Test 1: `sendChatMessage` posts to `/api/chat`, parses `ChatResponseSchema`, and converts invalid/error responses to Korean non-fabrication UI error objects.
    - Test 2: `fetchRecommendations` posts profile/session data to `/api/recommendations`, parses `RecommendationResponseSchema`, and preserves citations, deadline status, privacy metadata, and item order.
    - Test 3: preference helpers read/set/update/clear `/api/preferences` using a generated session key and parse `PreferenceStateSchema`.
    - Test 4: labels map exactly `active|expired|unknown`, `personalized_match|partial_match|general_recommendation`, and refusal tiers to UI-SPEC Korean copy and class variants.
  </behavior>
  <action>Create typed browser helpers `sendChatMessage`, `fetchRecommendations`, `readPreferences`, `savePreferences`, `updatePreferences`, and `clearPreferences` that use `fetch`, `content-type: application/json`, and existing Zod schemas before returning data to components. Create `getOrCreateSessionKey(storage = window.sessionStorage)` using prefix `erica-career-chat:` and key name `phase5-session-key`; it must not use localStorage for chat history. Create `deadline-labels.ts` exporting exact maps: `active` label `모집중`, `expired` label `마감됨`, `unknown` label `마감일 확인 필요`; match labels `맞춤 추천`, `일부 조건 일치`, `일반 안내`; refusal notice labels and copy from UI-SPEC lines 203-204. Keep errors as UI-safe Korean messages and never expose raw provider errors or secrets. This supports D-07, D-12, D-20, UX-02, UX-03, and UX-04.</action>
  <verify>
    <automated>npm test -- lib/api-client.test.ts lib/deadline-labels.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -n '모집중\|마감됨\|마감일 확인 필요\|맞춤 추천\|일부 조건 일치\|일반 안내\|답변 근거 부족\|확인 필요' lib/deadline-labels.ts` returns matches. Grep: `grep -n 'ChatResponseSchema\|RecommendationResponseSchema\|PreferenceStateSchema\|phase5-session-key\|sessionStorage' lib/api-client.ts lib/session-key.ts` returns matches. Grep excluding comments: `grep -R -v '^#' lib | grep -c 'localStorage\|dangerouslySetInnerHTML\|OPENAI_COMPAT_API_KEY'` outputs `0`.</acceptance_criteria>
  <done>Components can call validated API helpers and render semantic Korean labels without duplicating source/ranking/privacy logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→`app/api/*` | Untrusted client input enters route handlers. |
| Route handlers→existing services | Route wrappers invoke RAG/recommendation/preference services and must preserve contracts. |
| Services→Browser JSON | Source-grounded data with citations/freshness crosses to UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | `app/api/chat/route.ts` | mitigate | Parse with `ChatRequestSchema`; parse output with `ChatResponseSchema`; no streaming or raw source text. |
| T-05-05 | I | API error handling | mitigate | Return controlled Korean error JSON; redact/omit provider stack traces and `OPENAI_COMPAT_*` values. |
| T-05-06 | I | `app/api/preferences/route.ts` | mitigate | Default to session-first state; persistence only through existing consent gate; no localStorage chat history. |
| T-05-07 | S | Recommendation/source data | mitigate | Preserve official URLs, citations, fetched/posted dates, and deadline status so UI cannot spoof unlabeled recommendations. |
</threat_model>

<verification>
Run route/helper tests, `npm run typecheck`, `npm test`, and the grep acceptance gates.
</verification>

<success_criteria>
Next API and client helpers expose real Phase 3/4 service behavior to the UI with schema validation, safe errors, semantic labels, and no deferred scope.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-facing-experience/05-02-SUMMARY.md`.
</output>
