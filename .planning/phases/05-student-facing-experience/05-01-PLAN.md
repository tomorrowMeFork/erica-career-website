---
phase: 05-student-facing-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next-env.d.ts
  - next.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components.json
  - components/dashboard/student-dashboard.tsx
autonomous: true
requirements: [UX-01, UX-05]
must_haves:
  truths:
    - "Next.js App Router serves a Korean-first single dashboard shell."
    - "The visual baseline uses calm white surfaces, rounded cards, pill controls, Pretendard-first typography, and muted Hanyang-blue accents."
  artifacts:
    - path: "app/layout.tsx"
      provides: "Korean root layout, metadata, and global CSS wiring"
    - path: "app/page.tsx"
      provides: "Dashboard route entry"
    - path: "app/globals.css"
      provides: "Tailwind v4 import, UI-SPEC tokens, typography, focus, badge, and layout baseline"
    - path: "components/dashboard/student-dashboard.tsx"
      provides: "Client dashboard placeholder contract for later UI plans"
  key_links:
    - from: "app/page.tsx"
      to: "components/dashboard/student-dashboard.tsx"
      via: "StudentDashboard import/render"
      pattern: "StudentDashboard"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "global CSS import"
      pattern: "./globals.css"
---

<objective>
Create the Phase 5 frontend foundation: Next.js App Router, Tailwind v4/shadcn baseline, Korean root shell, and approved UI-SPEC design tokens.

Purpose: Phase 5 cannot build chat, citations, listings, or preferences until JSX/DOM/Next/Tailwind configuration exists without breaking the existing strict TypeScript service project.
Output: A buildable app shell that implements D-01, D-02, D-17, D-18, D-19, D-20 and supports UX-01/UX-05.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-student-facing-experience/05-CONTEXT.md
@.planning/phases/05-student-facing-experience/05-RESEARCH.md
@.planning/phases/05-student-facing-experience/05-PATTERNS.md
@.planning/phases/05-student-facing-experience/05-UI-SPEC.md
@package.json
@tsconfig.json

<interfaces>
Use the UI-SPEC exact values: font stack `Pretendard`, `Noto Sans KR`, `Apple SD Gothic Neo`, `system-ui`, `sans-serif`; `<html lang="ko">`; colors `oklch(0.99 0.004 250)`, `oklch(0.965 0.008 250)`, `oklch(0.48 0.13 252)`, `oklch(0.20 0.02 250)`, `oklch(0.42 0.025 250)`, `oklch(0.90 0.01 250)`; radii `8px`, `16px`, `24px`, `32px`, `9999px`; desktop source rail `360px`, wide source rail `400px`, left panel `280px`, container max `1440px`; copy `무엇을 도와드릴까요?` and `채용 공고, 마감일, 취업 프로그램을 한국어로 질문하면 확인된 출처를 함께 보여드려요.`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Next/Tailwind/shadcn baseline without breaking NodeNext services</name>
  <files>package.json, tsconfig.json, next-env.d.ts, next.config.ts, postcss.config.mjs, components.json</files>
  <read_first>package.json, tsconfig.json, .planning/phases/05-student-facing-experience/05-RESEARCH.md, .planning/phases/05-student-facing-experience/05-PATTERNS.md, .planning/phases/05-student-facing-experience/05-UI-SPEC.md</read_first>
  <action>Add runtime dependencies `next@16.2.4`, `react@19.2.5`, `react-dom@19.2.5`, `tailwindcss@4.2.4`, `@tailwindcss/postcss@4.2.4`, `postcss@8.5.13`, `lucide-react@1.14.0`; add dev dependencies `@types/react@19.2.14`, `@types/react-dom@19.2.3`, `@testing-library/react@16.3.2`, `@testing-library/user-event@14.6.1`, `jsdom@29.1.1`, `@playwright/test@1.59.1`, `eslint@10.3.0`, `eslint-config-next@16.2.4`. Add scripts exactly: `dev: next dev`, `build:web: next build`, `start:web: next start`, `test:ui: vitest run components app lib --environment jsdom`, `qa:web: playwright test`. Preserve existing `test`, `typecheck`, `evaluate:*`, `chat:smoke`, and NodeNext settings. Update `tsconfig.json` with `jsx: "preserve"`, `lib: ["dom", "dom.iterable", "es2022"]`, `baseUrl: "."`, `paths: { "@/*": ["./*"] }`, and includes for `next-env.d.ts`, `app/**/*.ts`, `app/**/*.tsx`, `components/**/*.ts`, `components/**/*.tsx`, `lib/**/*.ts`, existing `src/**/*.ts`, and `scripts/**/*.ts`. Create `next.config.ts`, `postcss.config.mjs` using `@tailwindcss/postcss`, and `components.json` configured with `tsx: true`, `tailwind.css: "app/globals.css"`, `tailwind.cssVariables: true`, aliases `@/components`, `@/components/ui`, `@/lib/utils`, and no third-party registries. Do not change `module`, `moduleResolution`, `type: "module"`, or existing `.js` service import style; this implements D-01 and D-17.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>CLI: `node -e "const p=require('./package.json'); for (const s of ['dev','build:web','start:web','test:ui','qa:web']) if(!p.scripts[s]) throw new Error(s); for (const d of ['next','react','react-dom','tailwindcss','@tailwindcss/postcss','postcss','lucide-react']) if(!p.dependencies[d]) throw new Error(d);"` passes. Grep: `grep -n '"module": "NodeNext"\|"moduleResolution": "NodeNext"\|"jsx": "preserve"\|"dom.iterable"' tsconfig.json` shows all required TypeScript settings. Grep: `grep -n '@tailwindcss/postcss' postcss.config.mjs` returns a match.</acceptance_criteria>
  <done>Next/Tailwind configuration exists, existing service tests/typecheck still run, and the project can accept App Router TSX files without changing service module semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Create Korean root layout and UI-SPEC global tokens</name>
  <files>app/layout.tsx, app/page.tsx, app/globals.css, components/dashboard/student-dashboard.tsx</files>
  <read_first>app/layout.tsx, app/page.tsx, app/globals.css, components/dashboard/student-dashboard.tsx, .planning/phases/05-student-facing-experience/05-UI-SPEC.md, DESIGN.md, .planning/research/design-seed.md</read_first>
  <action>Create `app/layout.tsx` with `import "./globals.css"`, metadata title `ERICA Career Chat`, metadata description `한양대 ERICA 취업 정보를 출처와 함께 확인하는 한국어 커리어 챗`, `<html lang="ko">`, and body font class using the exact UI-SPEC stack. Create `app/page.tsx` as a server component rendering `<StudentDashboard />`. Create `components/dashboard/student-dashboard.tsx` as a client component placeholder with dashboard header, empty chat guidance card, session storage scope chip, and Korean copy from UI-SPEC; it must not call APIs or persist chat yet. Create `app/globals.css` with `@import "tailwindcss"`, CSS variables for all UI-SPEC color/radius/layout tokens, `word-break: keep-all` for Korean prose, `:focus-visible` ring `2px solid var(--accent)` with `2px` offset, 44px minimum touch target utilities, white/soft-gray surfaces, rounded cards, pill controls, and no Meta brand names, Oculus purple, product-commerce copy, or Optimistic font references. This implements D-02, D-17, D-18, D-19, and D-20.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build:web</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -n 'lang="ko"\|ERICA Career Chat\|한양대 ERICA 취업 정보를 출처와 함께 확인' app/layout.tsx` returns matches. Grep: `grep -n 'oklch(0.99 0.004 250)\|oklch(0.48 0.13 252)\|Pretendard\|Noto Sans KR\|Apple SD Gothic Neo\|word-break: keep-all\|focus-visible\|44px' app/globals.css` returns matches. Grep excluding comments: `grep -v '^#' app/globals.css | grep -c 'Oculus\|Optimistic\|Meta branding'` outputs `0`. Grep: `grep -n '무엇을 도와드릴까요\|현재 세션에만 저장\|StudentDashboard' components/dashboard/student-dashboard.tsx app/page.tsx` returns matches.</acceptance_criteria>
  <done>The app opens to a Korean-first single dashboard shell using approved Phase 5 visual tokens and no prohibited Meta-commerce branding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Next app shell | Public UI renders user-facing Korean copy and later untrusted service data. |
| Package/config→runtime | New framework configuration must not weaken existing service tests or leak environment details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `app/globals.css`, `app/layout.tsx` | mitigate | Render only static Korean shell copy in this plan; no `dangerouslySetInnerHTML`; preserve future plain-text rendering posture. |
| T-05-02 | S | `app/layout.tsx` copy | mitigate | Use product name `ERICA Career Chat`; do not claim official Hanyang endorsement or SSO. |
| T-05-03 | I | `package.json`, config files | mitigate | Do not add env values or secrets; preserve `.env` handling from prior phases. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm test`, and `npm run build:web`. Confirm all acceptance grep gates pass.
</verification>

<success_criteria>
Phase 5 foundation exists with valid Next App Router shell, Tailwind v4 CSS tokens, shadcn config baseline, Korean root language, and no scope creep into API/chat/listing implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-facing-experience/05-01-SUMMARY.md`.
</output>
