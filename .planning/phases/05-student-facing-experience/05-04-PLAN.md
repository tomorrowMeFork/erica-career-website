---
phase: 05-student-facing-experience
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - components/listings/listing-panel.tsx
  - components/listings/listing-filter-pills.tsx
  - components/listings/listing-card.tsx
  - components/listings/deadline-status-badge.tsx
  - components/listings/match-strength-badge.tsx
  - components/listings/match-reason-list.tsx
  - components/preferences/preference-panel.tsx
  - components/preferences/preference-required-fields.tsx
  - components/preferences/preference-optional-accordion.tsx
  - components/preferences/storage-scope-chip.tsx
  - components/preferences/settings-menu.tsx
  - components/listings/listing-components.test.tsx
  - components/preferences/preference-components.test.tsx
autonomous: true
requirements: [UX-03, UX-04, UX-05]
must_haves:
  truths:
    - "User can browse latest or recommended employment listings outside the chat flow within the single dashboard."
    - "Listing cards visibly label active, expired, and unknown deadline status in Korean."
    - "Preference controls show required fields first, optional fields under progressive disclosure, session-first storage scope, and reachable clear/update controls."
    - "Listing and preference UI uses rounded cards, pill filters, semantic badges, and Korean-first typography."
  artifacts:
    - path: "components/listings/listing-panel.tsx"
      provides: "Recommended/latest listing browse panel"
    - path: "components/listings/listing-filter-pills.tsx"
      provides: "Core Phase 5 pill filters only"
    - path: "components/listings/listing-card.tsx"
      provides: "Citation-preserving employment listing card"
    - path: "components/preferences/preference-panel.tsx"
      provides: "Session-first preference side panel/drawer content"
    - path: "components/preferences/settings-menu.tsx"
      provides: "Clear/update/storage scope controls"
  key_links:
    - from: "components/listings/listing-panel.tsx"
      to: "lib/api-client.ts"
      via: "callback contract only: `onRefresh` prop; dashboard calls `fetchRecommendations` and passes items in (concrete wiring in 05-05 Task 1)"
      pattern: "onRefresh|sessionKey"
    - from: "components/listings/deadline-status-badge.tsx"
      to: "lib/deadline-labels.ts"
      via: "deadline label helpers"
      pattern: "getDeadlineStatus"
    - from: "components/preferences/preference-panel.tsx"
      to: "lib/api-client.ts"
      via: "callback contracts only: `onSave`/`onUpdate`/`onClear`/`onRead` props; dashboard invokes API helpers and passes state back (concrete wiring in 05-05 Task 1)"
      pattern: "onSave|onUpdate|onClear|onRead|sessionKey"
---

<objective>
Build listing browsing and preference controls for the single dashboard.

Purpose: UX-03 and UX-04 require students to browse recommended/latest employment listings outside chat and clearly see expired/uncertain posting labels, while Phase 4 privacy decisions require explicit preference controls.
Output: Listing cards/pill filters/status badges/match reasons plus preference panel/settings components with tests implementing D-09 through D-16 and D-20.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-student-facing-experience/05-CONTEXT.md
@.planning/phases/05-student-facing-experience/05-UI-SPEC.md
@.planning/phases/05-student-facing-experience/05-PATTERNS.md
@.planning/phases/04-personalization-and-recommendations/04-CONTEXT.md
@.planning/phases/04-personalization-and-recommendations/04-04-SUMMARY.md
@src/recommendations/recommendation-contract.ts
@src/personalization/preference-contract.ts
@lib/api-client.ts
@lib/deadline-labels.ts

<interfaces>
Listing filters are exactly `전체`, `추천`, `최신`, `마감 임박`, `출처`, `상태`; no advanced search. `ListingCard` uses `RecommendationItem` fields `title`, `category`, `url`, `posted_at`, `fetched_at`, `deadline_status`, `score`, `match_strength`, `match_reasons?`, `citations`. Deadline labels exactly `모집중`, `마감됨`, `마감일 확인 필요`. Match labels exactly `맞춤 추천`, `일부 조건 일치`, `일반 안내`. Preference required labels exactly `전공`, `희망 직무`; optional accordion label `선택 조건 더보기`; storage copy `현재 세션에만 저장`, `저장 안 함`, `동의 후 저장`; clear preferences confirmation `현재 세션의 추천 조건을 지울까요? 맞춤 추천이 꺼지고 기본 추천으로 돌아갑니다.`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build recommendation/latest listing browse panel with core filters and status badges</name>
  <files>components/listings/listing-panel.tsx, components/listings/listing-filter-pills.tsx, components/listings/listing-card.tsx, components/listings/deadline-status-badge.tsx, components/listings/match-strength-badge.tsx, components/listings/match-reason-list.tsx, components/listings/listing-components.test.tsx</files>
  <read_first>components/listings/listing-panel.tsx, components/listings/listing-filter-pills.tsx, components/listings/listing-card.tsx, components/listings/deadline-status-badge.tsx, components/listings/match-strength-badge.tsx, components/listings/match-reason-list.tsx, src/recommendations/recommendation-contract.ts, lib/deadline-labels.ts, .planning/phases/05-student-facing-experience/05-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: listing panel renders service-provided item order as recommended-first by default and labels no-preference mode as `기본 추천`.
    - Test 2: filter pills are exactly `전체`, `추천`, `최신`, `마감 임박`, `출처`, `상태` and do not render text search or advanced filters.
    - Test 3: listing cards show title, category, official link, posted/fetched dates, deadline status badge, score/match strength, up to 3 match reasons, and citation links.
    - Test 4: active/expired/unknown deadline badges render `모집중`, `마감됨`, `마감일 확인 필요`; expired cards are visually muted but readable and openable.
    - Test 5: `ListingPanel` accepts an `onRefresh` callback and a `sessionKey` prop; when `onRefresh` is called the dashboard invokes `fetchRecommendations` and feeds updated items back to the panel.
  </behavior>
  <action>Implement `ListingPanel` receiving `items`, `activeFilter`, `preferenceMode`, `privacyMetadata`, loading/empty states, and service-provided order. Implement `ListingFilterPills` with only six core filters: `전체`, `추천`, `최신`, `마감 임박`, `출처`, `상태`; filters are visual subsets over response data and must not implement advanced text search. Implement `ListingCard` with rounded white card, official link `공식 페이지 열기` using `target="_blank"` and `rel="noopener noreferrer"`, metadata rows for `posted_at`/`fetched_at`, `DeadlineStatusBadge`, `MatchStrengthBadge`, `MatchReasonList`, and citation count/source links. Use `deadline_status` enum only; do not infer deadlines from titles/raw text. Do not show `text`, `raw_text`, `cleaned_text`, or `session_only_optional_text`. This implements D-09, D-10, D-11, D-12, and D-20.</action>
  <verify>
    <automated>npm test -- components/listings/listing-components.test.tsx --environment jsdom</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -R -n '전체\|추천\|최신\|마감 임박\|출처\|상태\|모집중\|마감됨\|마감일 확인 필요\|맞춤 추천\|일부 조건 일치\|일반 안내\|공식 페이지 열기' components/listings` returns matches. Grep: `grep -R -n 'posted_at\|fetched_at\|deadline_status\|match_strength\|match_reasons\|citations\|noopener noreferrer' components/listings` returns matches. Grep excluding comments: `grep -R -v '^#' components/listings | grep -c 'advanced\|search input\|raw_text\|cleaned_text\|session_only_optional_text\|saved job\|reminder'` outputs `0`.</acceptance_criteria>
  <done>Listings are browsable outside chat with recommended/latest ordering, only core pill filters, citation-preserving cards, and visible Korean deadline labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build session-first preference panel, progressive optional fields, and clear/settings controls</name>
  <files>components/preferences/preference-panel.tsx, components/preferences/preference-required-fields.tsx, components/preferences/preference-optional-accordion.tsx, components/preferences/storage-scope-chip.tsx, components/preferences/settings-menu.tsx, components/preferences/preference-components.test.tsx</files>
  <read_first>components/preferences/preference-panel.tsx, components/preferences/preference-required-fields.tsx, components/preferences/preference-optional-accordion.tsx, components/preferences/storage-scope-chip.tsx, components/preferences/settings-menu.tsx, src/personalization/preference-contract.ts, lib/api-client.ts, .planning/phases/05-student-facing-experience/05-UI-SPEC.md, .planning/phases/04-personalization-and-recommendations/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: preference panel shows required fields first with labels `전공` and `희망 직무`; save button `추천 조건 저장` is disabled until both are non-empty.
    - Test 2: optional fields `industry`, `region`, `employment_type`, `deadline_sensitivity`, `session_only_optional_text` are hidden under `선택 조건 더보기` and session-only text is labeled not persistable.
    - Test 3: storage scope chip shows `현재 세션에만 저장`, `저장 안 함`, or `동의 후 저장`; default copy never implies inferred profiling.
    - Test 4: settings menu exposes clear preferences and clear chat history controls with the exact Korean confirmation copy and safe cancel default.
    - Test 5: `PreferencePanel` accepts `onSave`, `onUpdate`, `onClear`, `onRead` callbacks and a `sessionKey` prop matching `savePreferences`, `updatePreferences`, `clearPreferences`, `readPreferences` API signatures; the dashboard invokes these helpers and feeds resulting state back to the panel.
  </behavior>
  <action>Implement `PreferencePanel` as desktop side panel content and mobile-drawer-ready content; wire callbacks `onSet`, `onUpdate`, `onClear` to Phase 2 API helpers. Implement `PreferenceRequiredFields` for `major` and `target_role` only, labels `전공` and `희망 직무`, and save CTA `추천 조건 저장`. Implement `PreferenceOptionalAccordion` label `선택 조건 더보기` with optional arrays `industry`, `region`, `employment_type`, `deadline_sensitivity`, and `session_only_optional_text`; copy must say optional free text is session-only and not persistable. Implement `StorageScopeChip` for `session`, `none`, and persistent opt-in display but do not expose persistent opt-in unless consent timestamp, retention days, and deletion support fields are included. Implement `SettingsMenu` with `추천 조건 지우기` and `대화 기록 지우기` confirmations. Use copy `입력한 조건`, never `분석한 관심사`. This implements D-13, D-14, D-15, D-16, and prior Phase 4 privacy decisions.</action>
  <verify>
    <automated>npm test -- components/preferences/preference-components.test.tsx --environment jsdom</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -R -n '전공\|희망 직무\|추천 조건 저장\|선택 조건 더보기\|현재 세션에만 저장\|추천 조건 지우기\|대화 기록 지우기\|입력한 조건' components/preferences` returns matches. Grep: `grep -R -n 'major\|target_role\|industry\|region\|employment_type\|deadline_sensitivity\|session_only_optional_text\|storage_scope' components/preferences` returns matches. Grep excluding comments: `grep -R -v '^#' components/preferences | grep -c '분석한 관심사\|inferred\|localStorage\|server.*chat.*history'` outputs `0`.</acceptance_criteria>
  <done>Preference UI supports explicit session-first personalization with reachable update/clear controls and progressive optional fields without persistence or inferred profiling creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recommendation JSON→Listing UI | Source-grounded listings with citation metadata render in browser. |
| Student preference input→Preference API | Personal data enters preference lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | T | `ListingCard` | mitigate | Render service-provided fields only; preserve citations/deadline status; do not infer status or expose raw source text. |
| T-05-12 | I | `PreferencePanel` | mitigate | Required explicit fields first; optional free text labeled session-only; no localStorage or default persistent opt-in. |
| T-05-13 | R | `SettingsMenu` | mitigate | Clear actions require confirmation copy and visible storage scope; clear callback disables preference ranking. |
</threat_model>

<verification>
Run listing/preference component jsdom tests, `npm run typecheck`, `npm run test:ui`, and grep acceptance gates.
</verification>

<success_criteria>
Student dashboard has a listing browse surface and preference controls that satisfy UX-03/UX-04/UX-05 while respecting explicit preference privacy constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-facing-experience/05-04-SUMMARY.md`.
</output>
