---
phase: 05-student-facing-experience
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - components/dashboard/student-dashboard.tsx
  - components/chat/chat-composer.tsx
  - components/chat/chat-message-list.tsx
  - components/chat/user-message.tsx
  - components/chat/assistant-answer.tsx
  - components/chat/refusal-notice-card.tsx
  - components/chat/answer-attached-recommendations.tsx
  - components/citations/citation-trigger.tsx
  - components/citations/inline-citation-marker.tsx
  - components/citations/source-card.tsx
  - components/citations/source-inspection-rail.tsx
  - components/citations/mobile-source-sheet.tsx
  - components/chat/chat-components.test.tsx
  - components/citations/citation-components.test.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-04, UX-05]
must_haves:
  truths:
    - "User can ask a Korean question from a responsive chat dashboard and see client-session answer history."
    - "Complete-response loading, hard-refusal, and soft-hedge states render as gentle Korean notice cards."
    - "User can inspect source cards from an answer in-place without losing chat context."
    - "Source cards preserve citation IDs, title, URL, posted/fetched dates, page number, deadline badge, and trace ID context."
  artifacts:
    - path: "components/dashboard/student-dashboard.tsx"
      provides: "Client state coordinator for chat history, selected citations, panels, and session clearing"
    - path: "components/chat/chat-composer.tsx"
      provides: "Korean complete-response composer"
    - path: "components/chat/assistant-answer.tsx"
      provides: "Safe answer rendering with refusal notices, citation markers, confidence, and trace ID"
    - path: "components/citations/source-card.tsx"
      provides: "Trustworthy source card with freshness and deadline metadata"
    - path: "components/citations/source-inspection-rail.tsx"
      provides: "Desktop source inspection without navigation"
    - path: "components/citations/mobile-source-sheet.tsx"
      provides: "Mobile source inspection drawer/sheet without navigation"
  key_links:
    - from: "components/dashboard/student-dashboard.tsx"
      to: "lib/api-client.ts"
      via: "sendChatMessage and fetchRecommendations"
      pattern: "sendChatMessage|fetchRecommendations"
    - from: "components/chat/assistant-answer.tsx"
      to: "components/citations/inline-citation-marker.tsx"
      via: "citation marker callbacks"
      pattern: "InlineCitationMarker"
    - from: "components/citations/source-card.tsx"
      to: "lib/deadline-labels.ts"
      via: "deadline label map"
      pattern: "getDeadlineStatusLabel|getDeadlineStatusMeta"
---

<objective>
Implement the central student chat experience and citation/source inspection components from the approved UI-SPEC.

Purpose: UX-01 and UX-02 require users to ask questions, keep answer history, and inspect citations without losing context while preserving source metadata.
Output: Chat/dashboard components, citation rail/sheet/source cards, and component tests implementing D-02, D-04, D-05, D-06, D-07, D-08, D-12, D-17 through D-20.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/05-student-facing-experience/05-CONTEXT.md
@.planning/phases/05-student-facing-experience/05-UI-SPEC.md
@.planning/phases/05-student-facing-experience/05-PATTERNS.md
@src/chat/chat-contract.ts
@src/recommendations/recommendation-contract.ts
@lib/api-client.ts
@lib/deadline-labels.ts
@components/dashboard/student-dashboard.tsx

<interfaces>
Component contracts: `ChatComposer` props `query`, `onSubmit`, `isLoading`, `maxLength=2000`; `ChatMessageList` messages `{id, role, query?, response?, status}`; `AssistantAnswer` renders `answer`, `citations`, `refusal_tier`, `confidence`, `trace_id`; `InlineCitationMarker` aria-label `n번 출처 보기`; `CitationTrigger` copy `{count}개 출처 사용`; `SourceCard` props `citation_id`, `title`, `url`, `posted_at`, `fetched_at`, `deadline_status`, `page_number?`, `source_id`; official link label `{title} 공식 페이지 새 창으로 열기`, `target="_blank"`, `rel="noopener noreferrer"`. Hard refusal copy `확인된 근거가 부족해 답변할 수 없어요. 공식 출처에서 최신 정보를 확인해 주세요.` Soft hedge copy `일부 정보는 확인이 필요해요. 아래 출처와 날짜를 함께 확인해 주세요.` Loading copy `관련 출처를 확인하고 답변을 준비하고 있어요…`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build complete-response chat dashboard state and answer rendering</name>
  <files>components/dashboard/student-dashboard.tsx, components/chat/chat-composer.tsx, components/chat/chat-message-list.tsx, components/chat/user-message.tsx, components/chat/assistant-answer.tsx, components/chat/refusal-notice-card.tsx, components/chat/answer-attached-recommendations.tsx, components/chat/chat-components.test.tsx</files>
  <read_first>components/dashboard/student-dashboard.tsx, components/chat/chat-composer.tsx, components/chat/chat-message-list.tsx, components/chat/user-message.tsx, components/chat/assistant-answer.tsx, components/chat/refusal-notice-card.tsx, components/chat/answer-attached-recommendations.tsx, lib/api-client.ts, src/chat/chat-contract.ts, src/recommendations/recommendation-contract.ts, .planning/phases/05-student-facing-experience/05-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: empty dashboard shows `무엇을 도와드릴까요?`, Korean body copy, and focused/usable composer with `질문 보내기` disabled for whitespace.
    - Test 2: submitting `채용 공고 알려줘` appends a user message, shows loading copy, calls `sendChatMessage` once with `top_k: 5`, then on success calls `fetchRecommendations` with the same query and session key, attaches the returned `RecommendationItem[]` to the answer message by its answer id, and renders answer history with citations, confidence, trace ID, and recommendation cards below the answer text.
    - Test 3: Cmd/Ctrl+Enter submits; plain Enter keeps textarea behavior unless implementation labels otherwise.
    - Test 4: `hard_refuse` and `soft_hedge` responses render non-destructive notice cards with exact Korean copy and no red/destructive class.
    - Test 5: each attached recommendation card renders its `title`, `match_strength`, up to 3 `match_reasons`, `score`, and citation links; match reasons and citations are never omitted or detached from the card.
  </behavior>
  <action>Convert `StudentDashboard` into a client state coordinator owning `messages`, `query`, `isLoading`, `selectedCitationId`, `sourcePanelOpen`, and a clear-session-chat action that only clears browser client state. After a successful `sendChatMessage` response, `StudentDashboard` must call `fetchRecommendations` with the same query and session key, then attach the returned `RecommendationItem[]` to the answer message by its message id. Implement `ChatComposer` with maxLength 2000, sticky composer styles, `질문 보내기` primary button, loading disabled state, and `관련 출처를 확인하고 답변을 준비하고 있어요…` skeleton/notice. Implement message components that render answers as plain text or safe line breaks only; do not use `dangerouslySetInnerHTML`. `AssistantAnswer` must show inline numeric citation markers, `N개 출처 사용`, refusal notice card, confidence, and collapsed/metadata label `응답 추적 ID`. `AnswerAttachedRecommendations` renders compact cards from the attached `RecommendationItem[]`, each card showing `title`, `match_strength`, up to 3 `match_reasons`, `score`, and citation links; match reasons and citations must not be omitted or detached. Do not add server-side chat history persistence, streaming UI, fake chat answers, SSO, saved jobs, or resume/application tools. This implements D-02, D-04, D-06, D-07, and D-08.</action>
  <verify>
    <automated>npm test -- components/chat/chat-components.test.tsx --environment jsdom</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -R -n '질문 보내기\|관련 출처를 확인하고 답변을 준비하고 있어요\|응답 추적 ID\|확인된 근거가 부족\|일부 정보는 확인이 필요' components/dashboard components/chat` returns matches. Grep: `grep -R -n 'useState\|sendChatMessage\|top_k: 5\|fetchRecommendations\|session' components/dashboard/student-dashboard.tsx components/chat` returns matches. Grep: `grep -R -n 'match_reasons\|match_strength\|RecommendationItem' components/chat/answer-attached-recommendations.tsx components/chat/assistant-answer.tsx` returns matches. Grep excluding comments: `grep -R -v '^#' components/dashboard components/chat | grep -c 'dangerouslySetInnerHTML\|EventSource\|ReadableStream\|localStorage\|server.*history'` outputs `0`.</acceptance_criteria>
  <done>Students can use a complete-response Korean chat flow with client-session answer history, loading states, refusal notices, citations, and trace metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build in-place citation markers, desktop rail, mobile sheet, and source cards</name>
  <files>components/citations/citation-trigger.tsx, components/citations/inline-citation-marker.tsx, components/citations/source-card.tsx, components/citations/source-inspection-rail.tsx, components/citations/mobile-source-sheet.tsx, components/citations/citation-components.test.tsx, components/dashboard/student-dashboard.tsx, components/chat/assistant-answer.tsx</files>
  <read_first>components/citations/citation-trigger.tsx, components/citations/inline-citation-marker.tsx, components/citations/source-card.tsx, components/citations/source-inspection-rail.tsx, components/citations/mobile-source-sheet.tsx, components/dashboard/student-dashboard.tsx, components/chat/assistant-answer.tsx, src/chat/chat-contract.ts, lib/deadline-labels.ts, .planning/phases/05-student-facing-experience/05-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: inline marker `[1]` is a keyboard button with `aria-label="1번 출처 보기"` and opens source inspection without route navigation.
    - Test 2: desktop rail renders `SourceInspectionRail`, selected citation highlight, close button, and source card fields for title, source id, URL domain, posted date, fetched date, page number, deadline badge, and official-page link.
    - Test 3: mobile sheet/drawer renders from the same citation list, has a close button first in tab order, and restores focus to the opener after close.
    - Test 4: official links use `target="_blank"`, `rel="noopener noreferrer"`, and accessible label `{title} 공식 페이지 새 창으로 열기`.
  </behavior>
  <action>Implement `CitationTrigger` copy `{count}개 출처 사용`, `InlineCitationMarker` with accent focus ring and callback state, `SourceCard` with `공식 출처` badge, title, official URL domain, `posted_at` when non-null, `fetched_at`, `page_number` when present, `source_id`, deadline badge from `lib/deadline-labels.ts`, and `공식 페이지 열기` external link. Implement `SourceInspectionRail` as desktop right rail using width token `360px`/`400px` at wide viewport, and `MobileSourceSheet` as bottom drawer/sheet with focus management, Escape/close support, close button first, and restored focus. Wire `StudentDashboard` selected citation state into `AssistantAnswer` markers and source inspection. Do not navigate to a separate source route and do not hide citation freshness metadata. This implements D-05, D-12, D-20, UX-02, and UX-04.</action>
  <verify>
    <automated>npm test -- components/citations/citation-components.test.tsx --environment jsdom</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Grep: `grep -R -n '1번 출처 보기\|개 출처 사용\|공식 출처\|공식 페이지 열기\|noopener noreferrer\|target="_blank"\|마감 상태' components/citations components/chat components/dashboard` returns matches. Grep: `grep -R -n 'posted_at\|fetched_at\|page_number\|deadline_status\|source_id\|selectedCitationId\|sourcePanelOpen' components/citations components/dashboard components/chat` returns matches. Grep excluding comments: `grep -R -v '^#' components/citations components/dashboard components/chat | grep -c 'window.location\|router.push\|raw_text\|cleaned_text'` outputs `0`.</acceptance_criteria>
  <done>Citations are inspectable in-place on desktop and mobile with full source metadata, accessible controls, and no chat context loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON→React components | Untrusted answer/source titles and source metadata render in browser. |
| User interaction→source links | User opens official source URLs from citations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | T/I | `AssistantAnswer`, `SourceCard` | mitigate | Render answer/source fields safely as text; forbid `dangerouslySetInnerHTML`; keep raw source text out. |
| T-05-09 | S | `SourceCard` | mitigate | Show `공식 출처`, URL domain, source ID, posted/fetched dates, and deadline status; external link uses accessible label. |
| T-05-10 | D | `MobileSourceSheet` | mitigate | Provide close button, Escape handling, focus restoration, and no navigation trap. |
</threat_model>

<verification>
Run chat/citation component jsdom tests, `npm run typecheck`, `npm run test:ui`, and grep acceptance gates.
</verification>

<success_criteria>
The central dashboard chat can render complete source-grounded answers, gentle refusal states, answer-attached cards, and in-context source inspection on desktop/mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-facing-experience/05-03-SUMMARY.md`.
</output>
