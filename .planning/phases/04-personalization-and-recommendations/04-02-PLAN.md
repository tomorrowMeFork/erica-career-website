---
phase: 04-personalization-and-recommendations
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/recommendations/recommendation-contract.ts
  - src/recommendations/recommendation-contract.test.ts
  - src/recommendations/source-quality.ts
  - src/recommendations/source-quality.test.ts
  - src/recommendations/ranking.ts
  - src/recommendations/ranking.test.ts
autonomous: true
requirements: [PERS-02, PERS-04, SAFE-03]
user_setup: []
must_haves:
  truths:
    - "D-05/PERS-02: Recommendations use score-based ranking rather than strong filtering."
    - "D-06/PERS-04: No-preference ranking works and favors active/latest/source-grounded candidates."
    - "D-07/PERS-02: major and target_role contribute equal primary weight."
    - "D-08/PERS-02: freshness, active deadline status, detailed URLs, citation anchors, and boilerplate penalties affect ranking."
  artifacts:
    - path: "src/recommendations/recommendation-contract.ts"
      provides: "Recommendation request/item/response score contracts"
      exports: ["RecommendationRequestSchema", "RecommendationItemSchema", "RecommendationResponseSchema"]
    - path: "src/recommendations/source-quality.ts"
      provides: "Source specificity/freshness/deadline scoring over RetrievedChunk"
      exports: ["scoreSourceQuality"]
    - path: "src/recommendations/ranking.ts"
      provides: "Preference-aware and no-preference candidate ranking"
      exports: ["rankRecommendationCandidates", "scoreRecommendationCandidate"]
  key_links:
    - from: "src/recommendations/ranking.ts"
      to: "src/retrieval/retriever.ts"
      via: "Consumes RetrievedChunk.normalized_score and ranking_features"
      pattern: "RetrievedChunk"
    - from: "src/recommendations/source-quality.ts"
      to: "src/ingestion/normalized-record.ts"
      via: "Uses deadline_status, citation_anchors, source_url, posted_at, fetched_at"
      pattern: "deadline_status"
---

<objective>
Create deterministic recommendation ranking and score contracts.

Purpose: Recommendations must be explainably reordered by explicit preferences and source quality while preserving candidate visibility and no-preference behavior.
Output: Recommendation Zod contracts, source-quality scoring, and deterministic ranking functions over existing `RetrievedChunk` candidates.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-personalization-and-recommendations/04-CONTEXT.md
@.planning/phases/04-personalization-and-recommendations/04-RESEARCH.md
@.planning/phases/04-personalization-and-recommendations/04-PATTERNS.md
@.planning/phases/03-source-grounded-chat-mvp/03-02-SUMMARY.md
@src/retrieval/retriever.ts
@src/ingestion/normalized-record.ts
@src/personalization/preference-contract.ts

<interfaces>
Existing retrieval input:
```typescript
export type RetrievedChunk = {
  chunk: KnowledgeChunk;
  score: number;
  normalized_score: number;
  matched_terms: string[];
  ranking_features: {
    lexical_score: number;
    title_boost: number;
    category_boost: number;
    freshness_boost: number;
    deadline_penalty: number;
    boilerplate_penalty: number;
  };
};
```

Required scoring shape:
```typescript
score_breakdown: {
  base_retrieval_score: number;
  major_match_score: number;
  target_role_match_score: number;
  optional_preference_score: number;
  source_quality_score: number;
  freshness_score: number;
  final_score: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define recommendation contracts and source-quality scoring</name>
  <files>src/recommendations/recommendation-contract.ts, src/recommendations/recommendation-contract.test.ts, src/recommendations/source-quality.ts, src/recommendations/source-quality.test.ts</files>
  <read_first>
    <file>src/chat/chat-contract.ts</file>
    <file>src/retrieval/retriever.ts</file>
    <file>src/ingestion/normalized-record.ts</file>
    <file>src/personalization/preference-contract.ts</file>
    <file>.planning/phases/04-personalization-and-recommendations/04-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test D-08: active candidates with citation anchors and detailed URLs score higher than generic root/menu candidates with boilerplate penalty.
    - Test stale handling: expired listing-like candidates receive lower freshness/source-quality score than active candidates.
    - Test contract: `RecommendationItemSchema` requires structured citations compatible with `ChatCitationSchema`, match strength, score breakdown, and no raw source text field.
  </behavior>
  <action>Create `recommendation-contract.ts` exporting `MatchStrengthSchema` with values `personalized_match`, `partial_match`, and `general_recommendation`; `RecommendationRequestSchema` where `profile` is optional to satisfy D-06/PERS-04; `RecommendationItemSchema`; and `RecommendationResponseSchema`. Reuse `ChatCitationSchema` for citation metadata. Do not include `text`, `raw_text`, `cleaned_text`, or full source chunk text in normal recommendation item output. Create `source-quality.ts` with `scoreSourceQuality(candidate: RetrievedChunk, referenceDate?: Date): SourceQualityBreakdown`. It must reward `deadline_status === "active"`, recent `posted_at`/`fetched_at`, non-root/detail citation anchors, official HTTPS citation URLs already enforced by `KnowledgeChunkSchema`, and low `ranking_features.boilerplate_penalty`. It must not add new crawling or source parsing. Write tests before implementation.</action>
  <verify>
    <automated>npm test -- src/recommendations/recommendation-contract.test.ts src/recommendations/source-quality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/recommendations/recommendation-contract.ts` contains `RecommendationRequestSchema`.
    - `src/recommendations/recommendation-contract.ts` contains `ChatCitationSchema`.
    - `src/recommendations/recommendation-contract.ts` does not contain `raw_text`.
    - `src/recommendations/source-quality.ts` contains `scoreSourceQuality`.
    - `src/recommendations/source-quality.test.ts` contains `generic root`.
    - `npm test -- src/recommendations/recommendation-contract.test.ts src/recommendations/source-quality.test.ts` exits 0.
  </acceptance_criteria>
  <done>Recommendation contracts preserve citation/freshness metadata and source-quality scoring implements D-08 without source-scope expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement score-based preference and no-preference ranking</name>
  <files>src/recommendations/ranking.ts, src/recommendations/ranking.test.ts</files>
  <read_first>
    <file>src/recommendations/recommendation-contract.ts</file>
    <file>src/recommendations/source-quality.ts</file>
    <file>src/retrieval/retriever.ts</file>
    <file>src/personalization/preference-contract.ts</file>
    <file>.planning/phases/04-personalization-and-recommendations/04-CONTEXT.md</file>
  </read_first>
  <behavior>
    - Test D-05: candidates are sorted by final score but are not removed solely because a preference does not match.
    - Test D-06/PERS-04: `rankRecommendationCandidates({ candidates, profile: undefined })` returns recommendations ranked by active/latest/source quality and uses `general_recommendation` match strength.
    - Test D-07: swapping evidence so only `major` matches versus only `target_role` matches produces equal primary preference contribution.
    - Test D-08: when preference score ties, source-quality/freshness affects ordering.
  </behavior>
  <action>Create deterministic ranking functions. Export `scoreRecommendationCandidate` and `rankRecommendationCandidates`. Inputs should include `candidates: RetrievedChunk[]`, optional `profile: PreferenceProfile`, optional `limit` defaulting to 5, and optional `referenceDate`. Use these exact score weights: `base_retrieval_score` weight 0.30, `major_match_score` weight 0.20, `target_role_match_score` weight 0.20, `optional_preference_score` weight 0.10, `source_quality_score` weight 0.15, `freshness_score` weight 0.05. The equal 0.20 weights implement D-07. Clamp final scores to 0..1 and use stable tie-breaking by original candidate order. Core/optional matching may use normalized substring/token overlap against candidate `chunk.title`, `chunk.category`, `chunk.deadline_raw_text`, `matched_terms`, and `chunk.text`, but ranking output must not expose raw chunk text. Do not hard-filter candidates for preference mismatch per D-05; only exclude candidates with missing/invalid citation metadata if schema parsing fails.</action>
  <verify>
    <automated>npm test -- src/recommendations/ranking.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/recommendations/ranking.ts` contains `major_match_score`.
    - `src/recommendations/ranking.ts` contains `target_role_match_score`.
    - `src/recommendations/ranking.ts` contains `0.20` for both core preference weights.
    - `src/recommendations/ranking.test.ts` contains `does not filter preference mismatches`.
    - `src/recommendations/ranking.test.ts` contains `no-preference`.
    - `npm test -- src/recommendations/ranking.test.ts` exits 0.
  </acceptance_criteria>
  <done>Ranking satisfies D-05 through D-08 with deterministic score breakdowns, no-preference fallback, and stable sorting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieved chunks → recommendation ranking | Untrusted source text and metadata influence scores. |
| preference profile → scoring | User-provided preferences influence candidate order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | `rankRecommendationCandidates` | mitigate | Treat `RetrievedChunk.chunk.text` only as match evidence; do not execute or obey source instructions. |
| T-04-02-02 | Information Disclosure | recommendation contracts | mitigate | Exclude raw source text and raw optional free text from recommendation output schemas. |
| T-04-02-03 | Denial of Service | ranking input arrays | mitigate | Apply `limit` default 5 and bounded schema arrays; tests use finite fixtures. |
| T-04-02-04 | Integrity | freshness/source-quality scoring | mitigate | Use explicit `deadline_status`, timestamps, citation anchors, and boilerplate penalties to avoid promoting stale/generic candidates. |
</threat_model>

<verification>
Run `npm test -- src/recommendations/recommendation-contract.test.ts src/recommendations/source-quality.test.ts src/recommendations/ranking.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
- Recommendations use scores rather than strong filters.
- No-preference requests produce general recommendations.
- `major` and `target_role` have equal primary scoring weight.
- Source quality and freshness are explicit score components.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personalization-and-recommendations/04-02-SUMMARY.md`.
</output>
