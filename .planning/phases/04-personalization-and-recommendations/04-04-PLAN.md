---
phase: 04-personalization-and-recommendations
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - scripts/evaluate-personalization.ts
  - scripts/evaluate-personalization.test.ts
  - package.json
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, SAFE-03, SAFE-04, SAFE-06]
user_setup: []
must_haves:
  truths:
    - "Phase 4 behavior is covered by a deterministic local evaluation gate."
    - "Evaluation proves set/update/clear, no-preference fallback, reranking, citation-bearing Korean reasons, privacy gates, and stale/expired handling."
    - "The full project remains type-safe and existing Phase 3 RAG evaluation still passes."
  artifacts:
    - path: "scripts/evaluate-personalization.ts"
      provides: "Phase 4 deterministic evaluation CLI"
      exports: ["runPersonalizationEvaluation"]
    - path: "scripts/evaluate-personalization.test.ts"
      provides: "Source-inspected and behavior tests for the evaluation script"
    - path: "package.json"
      provides: "`evaluate:personalization` script"
      contains: "evaluate:personalization"
  key_links:
    - from: "scripts/evaluate-personalization.ts"
      to: "src/recommendations/recommendation-service.ts"
      via: "instantiates service with deterministic fixtures"
      pattern: "RecommendationService"
    - from: "package.json"
      to: "scripts/evaluate-personalization.ts"
      via: "npm script"
      pattern: "evaluate:personalization"
---

<objective>
Add the Phase 4 verification gate.

Purpose: Personalization touches privacy, ranking, Korean explanation quality, and citation integrity; a deterministic local gate prevents regressions before Phase 5 UI work consumes these contracts.
Output: Evaluation CLI, CLI tests, and package script.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-personalization-and-recommendations/04-CONTEXT.md
@.planning/phases/04-personalization-and-recommendations/04-RESEARCH.md
@.planning/phases/04-personalization-and-recommendations/04-PATTERNS.md
@.planning/phases/03-source-grounded-chat-mvp/03-07-SUMMARY.md
@package.json
@scripts/evaluate-rag-mvp.ts
@src/recommendations/recommendation-service.ts

<interfaces>
Existing package scripts:
```json
{
  "test": "vitest run",
  "typecheck": "tsc --noEmit",
  "evaluate:rag:mvp": "tsx scripts/evaluate-rag-mvp.ts"
}
```

New script target:
```json
"evaluate:personalization": "tsx scripts/evaluate-personalization.ts"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic personalization evaluation CLI</name>
  <files>scripts/evaluate-personalization.ts, scripts/evaluate-personalization.test.ts</files>
  <read_first>
    <file>scripts/evaluate-rag-mvp.ts</file>
    <file>src/recommendations/recommendation-service.ts</file>
    <file>src/personalization/preference-service.ts</file>
    <file>.planning/phases/04-personalization-and-recommendations/04-RESEARCH.md</file>
  </read_first>
  <behavior>
    - Test PERS-01/PERS-03: evaluation sets, updates, and clears preferences.
    - Test PERS-02: evaluation fails if preference changes do not alter explainable ranking.
    - Test PERS-04: evaluation passes a no-profile case with `general_recommendation` and citations.
    - Test SAFE-03/SAFE-06: evaluation fails if persistent write succeeds without consent or if raw free text appears in persisted state.
    - Test Korean/citation quality: every source-grounded reason contains Hangul and allowed `[n]` markers.
  </behavior>
  <action>Create `scripts/evaluate-personalization.ts` exporting `runPersonalizationEvaluation()` and executable as a CLI. Use deterministic in-file fixtures with `RetrievedChunk`-compatible candidates; do not read live data, crawl, or call model providers. Cover these named cases exactly: `preference lifecycle`, `major and target role reranking`, `no-preference fallback`, `weak match labeling`, `expired listing downranking`, `hostile source reason safety`, and `persistence consent gate`. On success print exactly `personalization evaluation passed`. On failure throw or exit non-zero with a safe error message that does not include raw optional preference free text. Write `scripts/evaluate-personalization.test.ts` to test exported evaluation and source-inspect that the success string exists.</action>
  <verify>
    <automated>npm test -- scripts/evaluate-personalization.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/evaluate-personalization.ts` contains `runPersonalizationEvaluation`.
    - `scripts/evaluate-personalization.ts` contains `personalization evaluation passed`.
    - `scripts/evaluate-personalization.ts` contains `hostile source reason safety`.
    - `scripts/evaluate-personalization.test.ts` contains `personalization evaluation passed`.
    - `npm test -- scripts/evaluate-personalization.test.ts` exits 0.
  </acceptance_criteria>
  <done>Phase 4 deterministic evaluation covers lifecycle, ranking, privacy, citation, stale/expired, and hostile-source cases.</done>
</task>

<task type="auto">
  <name>Task 2: Wire package script and run full verification</name>
  <files>package.json</files>
  <read_first>
    <file>package.json</file>
    <file>scripts/evaluate-personalization.ts</file>
  </read_first>
  <action>Add package script `"evaluate:personalization": "tsx scripts/evaluate-personalization.ts"` immediately after `"evaluate:rag:mvp"` or near existing evaluation scripts. Preserve existing scripts and JSON formatting. Then run targeted and full verification commands.</action>
  <verify>
    <automated>npm run evaluate:personalization && npm run typecheck && npm test && npm run evaluate:rag:mvp</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"evaluate:personalization": "tsx scripts/evaluate-personalization.ts"`.
    - `npm run evaluate:personalization` prints `personalization evaluation passed`.
    - `npm run typecheck` exits 0.
    - `npm test` exits 0.
    - `npm run evaluate:rag:mvp` exits 0.
  </acceptance_criteria>
  <done>Phase 4 verification is runnable through npm and the existing full project checks remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation fixtures → service contracts | Test fixtures simulate untrusted source chunks and preference input. |
| CLI output → developer terminal | Evaluation failures could leak preference text if not sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | `evaluate-personalization.ts` errors | mitigate | Do not print raw optional free text; use fixed safe fixture names and safe failure labels. |
| T-04-04-02 | Tampering | hostile source fixture | mitigate | Include hostile-source reason safety case verifying source instructions cannot remove citations or request personal data. |
| T-04-04-03 | Repudiation | evaluation results | mitigate | Named cases and deterministic assertions identify which Phase 4 invariant failed. |
| T-04-04-04 | Denial of Service | full verification command | accept | Local deterministic tests only; no network/model calls are introduced. |
</threat_model>

<verification>
Run `npm run evaluate:personalization && npm run typecheck && npm test && npm run evaluate:rag:mvp`.
</verification>

<success_criteria>
- A developer can run `npm run evaluate:personalization` and see `personalization evaluation passed`.
- The evaluation covers all Phase 4 requirements and locked decisions at behavior level.
- Existing Phase 3 RAG evaluation still passes after Phase 4 additions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-personalization-and-recommendations/04-04-SUMMARY.md`.
</output>
