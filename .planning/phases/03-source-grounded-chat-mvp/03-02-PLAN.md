---
phase: 03-source-grounded-chat-mvp
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/retrieval/retriever.ts
  - src/retrieval/normalize-korean.ts
  - src/retrieval/domain-synonyms.ts
  - src/retrieval/boilerplate-filter.ts
  - src/retrieval/bm25-retriever.ts
  - src/retrieval/retrieval-fixtures.test.ts
  - src/retrieval/boilerplate-filter.test.ts
autonomous: true
requirements: [RAG-02, RAG-04, RAG-06]
must_haves:
  truths:
    - "Decision traceability: D-01: BM25 Retriever interface; D-02: Korean normalization and char n-grams; D-03: all verified Phase 2 KB outputs; D-04: top 5 chunks; D-05: boilerplate exclusion; D-06: explicit synonym expansion; D-07: freshness/deadline ranking."
    - "Korean career-service and listing questions retrieve relevant source chunks from the Phase 2 KB."
    - "Boilerplate-only login, viewer-control, and UI-chrome chunks do not become answer evidence."
    - "Ranking accounts for active/recent listings without over-penalizing campus-service 안내 chunks with unknown deadlines."
  artifacts:
    - path: "src/retrieval/retriever.ts"
      provides: "Retriever interface with RetrieveInput and RetrievedChunk contracts"
      exports: ["Retriever", "RetrieveInput", "RetrievedChunk"]
    - path: "src/retrieval/bm25-retriever.ts"
      provides: "BM25-style lexical retriever with Korean char n-grams and ranking features"
      contains: "topK ?? 5"
    - path: "src/retrieval/boilerplate-filter.ts"
      provides: "answerable/mixed/boilerplate_only classifier"
      contains: "boilerplate_only"
  key_links:
    - from: "src/retrieval/bm25-retriever.ts"
      to: "src/knowledge-base/jsonl-loader.ts"
      via: "KnowledgeChunk[] input parsed by loader"
      pattern: "KnowledgeChunk"
    - from: "src/retrieval/domain-synonyms.ts"
      to: "src/retrieval/normalize-korean.ts"
      via: "explicit query expansion terms"
      pattern: "자소서"
---

<objective>
Implement the locked BM25-style Korean retrieval baseline behind a `Retriever` interface.

Purpose: Korean source-grounded chat needs deterministic retrieval over Phase 2 chunks before answer generation. This plan implements D-01 through D-08 and D-09 without adding embeddings, vector stores, or heavyweight morphology dependencies.
Output: Retrieval contracts, Korean normalization, explicit synonym expansion, boilerplate classifier, BM25-style retriever, and verification-first tests.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
@.planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
@.planning/phases/03-source-grounded-chat-mvp/03-PATTERNS.md
@src/knowledge-base/jsonl-loader.ts
@src/ingestion/normalized-record.ts
@src/ingestion/deadline-status.ts
@src/ingestion/chunking.ts

<interfaces>
Create this interface exactly in `src/retrieval/retriever.ts` per D-01:

```typescript
export type RetrieveInput = { query: string; topK?: number };
export type RetrievedChunk = {
  chunk: KnowledgeChunk;
  score: number;
  normalized_score: number;
  matched_terms: string[];
  ranking_features: {
    lexical_score: number;
    title_boost: number;
    category_boost: number;
    freshness_boost: number;
    deadline_penalty: number;
    boilerplate_penalty: number;
  };
};
export interface Retriever {
  retrieve(input: RetrieveInput): Promise<RetrievedChunk[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Korean normalization and explicit domain synonym expansion</name>
  <files>src/retrieval/retriever.ts, src/retrieval/normalize-korean.ts, src/retrieval/domain-synonyms.ts, src/retrieval/retrieval-fixtures.test.ts</files>
  <read_first>
    - src/retrieval/retriever.ts
    - src/retrieval/normalize-korean.ts
    - src/retrieval/domain-synonyms.ts
    - src/retrieval/retrieval-fixtures.test.ts
    - src/ingestion/normalized-record.ts
    - src/ingestion/deadline-status.ts
    - .planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
    - .planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
    - .planning/phases/03-source-grounded-chat-mvp/03-PATTERNS.md
  </read_first>
  <behavior>
    - Normalize with Unicode `NFKC`, lowercase Latin characters, trim/collapse whitespace, and simple punctuation cleanup per D-02.
    - Generate Hangul 2-grams and 3-grams from Korean sequences without adding a Korean morphology dependency per D-02.
    - Expand only the explicit D-06/domain terms: `채용/모집/공고`, `인턴/intern/인턴십`, `상담/컨설팅/컨설팅룸/상담예약`, `취업프로그램/직무부트캠프/프로그램`, `자기소개서/자소서/첨삭`, `가이드북/매뉴얼/사용자가이드`, `취업준비도검사/진로적성검사/진로설계/경력개발/포트폴리오`, `취업성공후기/선배/사례/인터뷰`.
  </behavior>
  <action>Create `src/retrieval/retriever.ts` with the interface block shown above. Create `src/retrieval/normalize-korean.ts` exporting `normalizeKoreanText(value: string): string`, `extractSearchTerms(value: string): string[]`, and `hangulNgrams(value: string, sizes = [2, 3]): string[]`. Create `src/retrieval/domain-synonyms.ts` exporting `expandDomainSynonyms(terms: readonly string[]): string[]` and a constant table containing only the D-06/domain service terms listed in this task. Add tests in `src/retrieval/retrieval-fixtures.test.ts` asserting `자소서` expands to `자기소개서`, `상담` expands to `컨설팅` and `상담예약`, `intern` expands to `인턴`, and `가이드북` expands to `매뉴얼`; assert Hangul 2/3-grams exist for `상담예약`.</action>
  <verify>
    <automated>npm test -- src/retrieval/retrieval-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export interface Retriever" src/retrieval/retriever.ts` returns exactly one match.
    - `grep -n "NFKC" src/retrieval/normalize-korean.ts` returns at least one match.
    - `grep -n "자소서\|상담예약\|직무부트캠프\|취업준비도검사" src/retrieval/domain-synonyms.ts` returns at least four matches.
    - `grep -n "mecab\|komoran\|okt\|kiwi" package.json src/retrieval/*.ts` returns no matches.
    - `npm test -- src/retrieval/retrieval-fixtures.test.ts` exits 0 for the synonym and n-gram tests introduced here.
  </acceptance_criteria>
  <done>Retrieval contracts and Korean query processing implement D-01, D-02, and D-06 with deterministic tests and no morphology dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic boilerplate classification</name>
  <files>src/retrieval/boilerplate-filter.ts, src/retrieval/boilerplate-filter.test.ts</files>
  <read_first>
    - src/retrieval/boilerplate-filter.ts
    - src/retrieval/boilerplate-filter.test.ts
    - src/ingestion/normalized-record.ts
    - .planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
    - .planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
    - .planning/phases/03-source-grounded-chat-mvp/03-PATTERNS.md
  </read_first>
  <behavior>
    - Classify chunks as `answerable`, `mixed`, or `boilerplate_only`.
    - `포털시스템 로그인`, `아이디/비밀번호`, `공유`, `인쇄`, `다운로드`, `전체화면`, `PRINT`, `목차`, `MAIN`, `HOME`, `exit_to_app`, and `subjectclose` are boilerplate signals per D-05.
    - Real campus-service/listing terms such as `상담예약`, `전문가 상담`, `컨설팅룸예약`, `취업프로그램`, `채용정보`, and `자기소개서 첨삭` keep a chunk as answerable or mixed, not boilerplate-only.
  </behavior>
  <action>Create `src/retrieval/boilerplate-filter.ts` with `BoilerplateClassificationSchema = z.object({ label: z.enum(["answerable", "mixed", "boilerplate_only"]), boilerplate_score: z.number(), answer_signal_score: z.number(), matched_boilerplate_signals: z.array(z.string()), matched_answer_signals: z.array(z.string()) })`, export its inferred type, and export `classifyBoilerplate(text: string): BoilerplateClassification`. Create `src/retrieval/boilerplate-filter.test.ts` with tests for login prompts, viewer controls, site chrome, mixed CDP service/menu text, and listing text. Directly implement D-05 and D-21 input/document sanitization by normalizing control characters before matching.</action>
  <verify>
    <automated>npm test -- src/retrieval/boilerplate-filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "boilerplate_only" src/retrieval/boilerplate-filter.ts src/retrieval/boilerplate-filter.test.ts` returns at least two matches.
    - `grep -n "포털시스템 로그인\|아이디/비밀번호\|다운로드\|전체화면\|PRINT" src/retrieval/boilerplate-filter.ts` returns at least five matches.
    - `grep -n "상담예약\|컨설팅룸예약\|자기소개서 첨삭" src/retrieval/boilerplate-filter.ts src/retrieval/boilerplate-filter.test.ts` returns at least three matches.
    - `npm test -- src/retrieval/boilerplate-filter.test.ts` exits 0.
  </acceptance_criteria>
  <done>Boilerplate-only content is deterministically excluded or penalizable before answer generation while source-grounded campus-service evidence remains retrievable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build BM25-style retriever with freshness/deadline ranking</name>
  <files>src/retrieval/bm25-retriever.ts, src/retrieval/retrieval-fixtures.test.ts</files>
  <read_first>
    - src/retrieval/bm25-retriever.ts
    - src/retrieval/retrieval-fixtures.test.ts
    - src/retrieval/retriever.ts
    - src/retrieval/normalize-korean.ts
    - src/retrieval/domain-synonyms.ts
    - src/retrieval/boilerplate-filter.ts
    - src/knowledge-base/jsonl-loader.ts
    - src/ingestion/normalized-record.ts
    - .planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
    - .planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
  </read_first>
  <behavior>
    - Default retrieval returns top 5 chunks per D-04.
    - Retrieval scores title/category/source fields with modest boosts; active/recent chunks receive a boost; expired listing-like chunks receive a penalty per D-07.
    - Queries for `현장실습 참여기업`, `상담예약`, `컨설팅룸예약`, `자기소개서 첨삭`, `취업준비도검사`, and `CDP 학생 가이드북` retrieve meaningful chunks from the default KB directories.
    - Boilerplate-only chunks are not returned as answer evidence unless every result is excluded, and then evidence policy will hard refuse in Plan 03.
  </behavior>
  <action>Create `src/retrieval/bm25-retriever.ts` exporting `Bm25Retriever` that implements `Retriever`, accepts `KnowledgeChunk[]` in its constructor, parses each chunk with `KnowledgeChunkSchema`, builds deterministic term frequencies from normalized text/title/category/source_name, expands query terms with `expandDomainSynonyms`, applies Hangul n-grams, computes a BM25-style lexical score, normalizes scores to `0..1`, and returns stable sorted `RetrievedChunk[]` with `ranking_features`. Add fixture tests in `src/retrieval/retrieval-fixtures.test.ts` that load chunks with `loadKnowledgeBaseChunks()`, instantiate `new Bm25Retriever(chunks)`, assert `retrieve({ query: "상담예약이나 전문가 상담은 어디서 확인해?" })` returns no more than 5 results and includes a result whose text/title contains `상담` or `컨설팅`, assert `retrieve({ query: "CDP 학생 가이드북" })` returns a citation anchor with `page_number` or `#page=1`, and assert boilerplate-only login/viewer-control results are downranked below answerable results for service queries.</action>
  <verify>
    <automated>npm test -- src/retrieval/retrieval-fixtures.test.ts src/retrieval/boilerplate-filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class Bm25Retriever" src/retrieval/bm25-retriever.ts` returns at least one match.
    - `grep -n "topK.*5\|5.*topK" src/retrieval/bm25-retriever.ts` returns at least one match.
    - `grep -n "freshness_boost\|deadline_penalty\|boilerplate_penalty" src/retrieval/bm25-retriever.ts` returns at least three matches.
    - `grep -n "상담예약\|CDP 학생 가이드북\|현장실습" src/retrieval/retrieval-fixtures.test.ts` returns at least three matches.
    - `npm test -- src/retrieval/retrieval-fixtures.test.ts src/retrieval/boilerplate-filter.test.ts` exits 0.
  </acceptance_criteria>
  <done>BM25-style retrieval satisfies Korean query handling, campus-service coverage, top-5 default behavior, boilerplate exclusion/downranking, and freshness/deadline ranking.</done>
</task>

</tasks>

<decision_traceability>
This plan provides explicit CONTEXT.md decision coverage for: D-01: BM25-style lexical baseline behind `Retriever` interface; D-02: Korean normalization plus character n-gram indexing without heavyweight morphology dependency; D-03: index all verified Phase 2 KB outputs; D-04: top 5 chunks by default; D-05: exclude login prompts, viewer controls, download buttons, repeated footers, UI chrome, and other non-answer boilerplate from answer retrieval; D-06: preserve original Korean query and use only explicit synonym expansions: 채용/모집/공고, 인턴/intern, 상담/컨설팅, 취업프로그램/직무부트캠프, 자기소개서/자소서, 가이드북/매뉴얼; D-07: boost active/recent chunks and penalize expired/stale chunks.
</decision_traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query text→retrieval scoring | User-controlled query text influences matching and ranking. |
| Source text→answer evidence set | Untrusted source chunks are selected before prompt construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `src/retrieval/bm25-retriever.ts` | mitigate | Re-parse constructor chunks with `KnowledgeChunkSchema` and preserve `source_text_trust` metadata. |
| T-03-02-02 | Information Disclosure | retrieval result shape | mitigate | Return `RetrievedChunk` only to internal chat orchestration; no user-facing endpoint is created in this plan. |
| T-03-02-03 | Elevation of Privilege | `src/retrieval/boilerplate-filter.ts` | mitigate | Classify source instructions, login prompts, and viewer controls as `boilerplate_only` so they cannot become instructions. |
| T-03-02-04 | Denial of Service | `src/retrieval/normalize-korean.ts` | mitigate | Use deterministic local tokenization and bounded `topK`; avoid network calls and heavyweight morphology dependencies. |
| T-03-02-05 | Spoofing | citation metadata in chunks | mitigate | Keep official URL/source fields from `KnowledgeChunk` unchanged; do not synthesize source titles or URLs during retrieval. |
</threat_model>

<verification>
Run:
- `npm test -- src/retrieval/retrieval-fixtures.test.ts src/retrieval/boilerplate-filter.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
Korean lexical retrieval returns meaningful Phase 2 chunks for listings, guidebook, and campus career-service 안내 questions; boilerplate-only source text is excluded/downranked; all results include normalized scores and ranking features for audit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-source-grounded-chat-mvp/03-02-SUMMARY.md`.
</output>
