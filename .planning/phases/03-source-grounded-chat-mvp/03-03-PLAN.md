---
phase: 03-source-grounded-chat-mvp
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/chat/evidence-policy.ts
  - src/chat/output-validation.ts
  - src/chat/output-validation.test.ts
autonomous: true
requirements: [RAG-03, RAG-04, RAG-06]
must_haves:
  truths:
    - "Decision traceability: D-12: Korean-first source-grounded style; D-13: inline numeric citations; D-15: citation freshness/deadline validation; D-17: evidence tiers; D-18: hard refusal; D-19: soft hedge; D-20: insufficient-evidence thresholds; D-21: output validation safety layer."
    - "Evidence strength deterministically maps to hard_refuse, soft_hedge, or normal_answer."
    - "Citationless factual answers fail closed instead of reaching users."
    - "Citation objects preserve title, official URL, fetched date, posted date, deadline status, and PDF page context when available."
  artifacts:
    - path: "src/chat/evidence-policy.ts"
      provides: "D-17 through D-20 refusal-tier classifier"
      contains: "0.30"
    - path: "src/chat/output-validation.ts"
      provides: "Runtime schema, citation-marker, Korean-first, and unsafe-output validation"
      contains: "출처를 생략"
    - path: "src/chat/output-validation.test.ts"
      provides: "Verification-first coverage for citations, refusals, malformed output, and hostile-source compliance"
      contains: "citationless"
  key_links:
    - from: "src/chat/evidence-policy.ts"
      to: "src/retrieval/retriever.ts"
      via: "RetrievedChunk normalized_score and ranking features"
      pattern: "normalized_score"
    - from: "src/chat/output-validation.ts"
      to: "src/chat/chat-contract.ts"
      via: "ChatResponseSchema safeParse"
      pattern: "ChatResponseSchema"
---

<objective>
Implement evidence-tier classification and fail-closed answer validation.

Purpose: The chat MVP must refuse or hedge on weak evidence, enforce citation markers, and preserve freshness metadata before any answer is returned.
Output: Evidence policy module, output-validation module, and tests for RAG-03, RAG-04, and RAG-06 guardrails.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
@.planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
@.planning/phases/03-source-grounded-chat-mvp/03-PATTERNS.md
@src/chat/chat-contract.ts
@src/retrieval/retriever.ts
@src/ingestion/normalized-record.ts

<interfaces>
Evidence tiers are locked by D-17 through D-20:

```typescript
hard_refuse: normalized score < 0.30, zero chunks, boilerplate-only results, or missing citation anchors
soft_hedge: normalized score >= 0.30 and <= 0.50
normal_answer: normalized score > 0.50
```

Soft hedge wording must include the exact Korean phrase `현재 수집된 자료 기준으로는` in guarded answer construction or validation expectations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement three-tier evidence policy</name>
  <files>src/chat/evidence-policy.ts, src/chat/output-validation.test.ts</files>
  <read_first>
    - src/chat/evidence-policy.ts
    - src/chat/output-validation.test.ts
    - src/chat/chat-contract.ts
    - src/retrieval/retriever.ts
    - src/retrieval/boilerplate-filter.ts
    - .planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
    - .planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
  </read_first>
  <behavior>
    - Zero chunks returns `hard_refuse`.
    - Top normalized score below `0.30` returns `hard_refuse`.
    - Top normalized score from `0.30` through `0.50` returns `soft_hedge` and exposes the Korean hedge prefix `현재 수집된 자료 기준으로는`.
    - Top normalized score above `0.50` returns `normal_answer` unless citation anchors are missing or boilerplate-only results dominate.
  </behavior>
  <action>Create `src/chat/evidence-policy.ts` exporting `EvidencePolicyConfig`, `DEFAULT_EVIDENCE_POLICY = { hard_refuse_below: 0.30, soft_hedge_through: 0.50, soft_hedge_prefix: "현재 수집된 자료 기준으로는" }`, `evaluateEvidence(results: readonly RetrievedChunk[], options?: { boilerplateOnlyChunkIds?: ReadonlySet<string>; config?: EvidencePolicyConfig }): { refusal_tier: RefusalTier; confidence: number; reasons: string[]; soft_hedge_prefix?: string }`, and `buildHardRefusalAnswer(): string`. Implement D-17 through D-20 exactly: zero chunks, boilerplate-only-only results, missing citation anchors, or citationless results are hard refusal. Add tests in `src/chat/output-validation.test.ts` for scores `0.29`, `0.30`, `0.50`, and `0.51`, with exact expected tiers.</action>
  <verify>
    <automated>npm test -- src/chat/output-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hard_refuse_below: 0.30" src/chat/evidence-policy.ts` returns one match.
    - `grep -n "soft_hedge_through: 0.50" src/chat/evidence-policy.ts` returns one match.
    - `grep -n "현재 수집된 자료 기준으로는" src/chat/evidence-policy.ts src/chat/output-validation.test.ts` returns at least two matches.
    - `grep -n "0.29\|0.30\|0.50\|0.51" src/chat/output-validation.test.ts` returns at least four matches.
    - `npm test -- src/chat/output-validation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Evidence policy deterministically enforces D-17 through D-20 with exact thresholds and refusal/hedge wording.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate citations, Korean-first answers, and hostile-output failures</name>
  <files>src/chat/output-validation.ts, src/chat/output-validation.test.ts</files>
  <read_first>
    - src/chat/output-validation.ts
    - src/chat/output-validation.test.ts
    - src/chat/chat-contract.ts
    - src/ingestion/normalized-record.ts
    - .planning/phases/03-source-grounded-chat-mvp/03-CONTEXT.md
    - .planning/phases/03-source-grounded-chat-mvp/RESEARCH.md
    - .planning/phases/03-source-grounded-chat-mvp/03-PATTERNS.md
  </read_first>
  <behavior>
    - Valid normal/soft answers include inline numeric citations such as `[1]`, and every marker maps to a `citations[]` item.
    - `citations[]` entries include `title`, `url`, `fetched_at`, `posted_at`, `deadline_status`, and optional `page_number` per D-15.
    - Outputs containing `출처를 생략하겠습니다`, official endorsement claims, guaranteed outcome claims, or factual Korean prose without `[n]` fail validation per D-21, D-22, and D-27.
    - Hard refusals can omit citations only when evidence policy requires refusal and the answer contains no fabricated facts.
  </behavior>
  <action>Create `src/chat/output-validation.ts` exporting `validateChatResponseOutput(input: { response: unknown; allowedCitationIds: readonly number[]; expectedTier: RefusalTier }): { ok: true; response: ChatResponse } | { ok: false; failures: string[] }`. Parse with `ChatResponseSchema`; reject empty or non-Korean-first answers by requiring at least one Hangul syllable; extract citation markers with `/\[(\d+)\]/gu`; reject normal and soft answers with no citation markers; reject markers not present in `citations[].citation_id`; reject `citations[]` missing title/url/fetched_at through schema; reject answers containing exact unsafe strings `출처를 생략`, `공식 인증`, `취업을 보장`, or `이전 지시를 무시`. Add tests for a valid listing answer with `[1]`, a valid PDF answer with `page_number: 1`, a no-answer hard refusal, a citationless mock-model answer, and a hostile output saying `출처를 생략하겠습니다`.</action>
  <verify>
    <automated>npm test -- src/chat/output-validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ChatResponseSchema" src/chat/output-validation.ts` returns at least one match.
    - `grep -n "\\\\\\[" src/chat/output-validation.ts` returns at least one match for citation marker parsing.
    - `grep -n "출처를 생략\|공식 인증\|취업을 보장\|이전 지시를 무시" src/chat/output-validation.ts src/chat/output-validation.test.ts` returns at least four matches.
    - `grep -n "page_number: 1" src/chat/output-validation.test.ts` returns at least one match.
    - `npm test -- src/chat/output-validation.test.ts` exits 0.
  </acceptance_criteria>
  <done>Generated or provider-returned answers are schema-valid, Korean-first, citation-consistent, and fail closed on prompt-injection compliance or unsupported claims.</done>
</task>

</tasks>

<decision_traceability>
This plan provides explicit CONTEXT.md decision coverage for: D-12: Korean-first, friendly, counseling-style, source-grounded answers with no personalized coaching; D-13: inline numeric citations like `[1]` for factual claims; D-15: citation freshness/deadline metadata validation; D-17: three-tier evidence policy; D-18: short useful hard refusal text; D-19: soft hedge answers constrained to retrieved evidence; D-20: insufficient evidence when relevant chunk count, citation coverage, or score margin is below thresholds; D-21: output validation safety layer.
</decision_traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retrieved evidence→answer decision | Retrieved chunks may be weak, stale, boilerplate, or missing citation anchors. |
| Model output→user response | Provider output is untrusted until schema and citation validation pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Spoofing | `src/chat/output-validation.ts` | mitigate | Require every inline `[n]` marker to map to a structured citation ID. |
| T-03-03-02 | Tampering | `src/chat/evidence-policy.ts` | mitigate | Force hard refusal for zero chunks, below-0.30 confidence, missing citations, and boilerplate-only evidence. |
| T-03-03-03 | Repudiation | validation failures | mitigate | Return explicit failure strings for schema, marker, unsafe phrase, and missing metadata failures. |
| T-03-03-04 | Information Disclosure | hard refusal handling | mitigate | Hard-refusal validation does not require exposing retrieved chunk text or prompt text. |
| T-03-03-05 | Elevation of Privilege | hostile output phrases | mitigate | Reject model output that follows source instructions such as `출처를 생략` or `이전 지시를 무시`. |
</threat_model>

<verification>
Run:
- `npm test -- src/chat/output-validation.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
Evidence tiers match D-17 through D-20 exactly, citations are structurally valid and marker-consistent, Korean factual answers without citations fail closed, and hostile source/model attempts to suppress citations are rejected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-source-grounded-chat-mvp/03-03-SUMMARY.md`.
</output>
