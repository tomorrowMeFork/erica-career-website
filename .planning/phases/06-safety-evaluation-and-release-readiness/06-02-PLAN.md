---
phase: 06-safety-evaluation-and-release-readiness
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - data/evaluation/phase6-reference-qa.ts
  - data/evaluation/phase6-reference-qa.test.ts
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-06]
user_setup: []
must_haves:
  truths:
    - "The project has a Korean reference QA dataset covering all seven locked Phase 6 categories."
    - "Every QA case describes expected retrieval behavior, answer characteristics, refusal tier, and freshness/deadline expectations where applicable."
    - "Hostile-source and no-answer cases are first-class dataset entries, not ad hoc test-only afterthoughts."
  artifacts:
    - path: "data/evaluation/phase6-reference-qa.ts"
      provides: "Typed Phase 6 QA dataset and schemas"
      exports: ["PHASE6_REFERENCE_QA_CASES", "Phase6QaCaseSchema", "Phase6QaCategorySchema"]
    - path: "data/evaluation/phase6-reference-qa.test.ts"
      provides: "Dataset coverage and schema tests"
  key_links:
    - from: "data/evaluation/phase6-reference-qa.ts"
      to: "scripts/evaluate-release-readiness.ts"
      via: "exported typed dataset consumed by Plan 03"
      pattern: "PHASE6_REFERENCE_QA_CASES"
---

<objective>
Create the Phase 6 reference QA dataset contract and coverage tests that the release-readiness evaluator will consume.

Purpose: EVAL-01 depends on a structured, deterministic, Korean-first dataset before retrieval/answer gates can be implemented without interpretation.
Output: Typed QA case schema, exported dataset, and tests proving category and field coverage.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-RESEARCH.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md
@src/chat/chat-contract.ts
@src/ingestion/normalized-record.ts
@scripts/evaluate-rag-mvp.ts
@scripts/evaluate-rag-mvp.test.ts

<interfaces>
Use the case shape from 06-PATTERNS: categories `cdp_usage`, `listing_deadline`, `success_story`, `guidebook_pdf`, `no_answer`, `personalization`, and `hostile_source`; include `question_ko`, expected top source/chunk IDs, expected refusal tier, required answer checks, freshness/deadline expectations, and prohibited unsafe-claim patterns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 6 QA schemas and typed case contract</name>
  <read_first>
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md` decisions D-10 through D-18
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md` section `data/evaluation/phase6-reference-qa.ts`
    - `src/chat/chat-contract.ts`
    - `src/ingestion/normalized-record.ts`
  </read_first>
  <files>data/evaluation/phase6-reference-qa.ts, data/evaluation/phase6-reference-qa.test.ts</files>
  <behavior>
    - Schema accepts only the locked QA categories from D-10.
    - Schema requires Korean questions, expected retrieval behavior, expected answer checks, refusal tiers, and optional freshness/deadline expectations (D-11).
    - Schema can represent no-answer/refusal and hostile-source expectations (D-12, D-16).
  </behavior>
  <action>Create `data/evaluation/phase6-reference-qa.ts` with Zod schemas and TypeScript types for the Phase 6 QA case contract. Export the schemas/types and an initially filled dataset in Task 2. Include answer-check enums for `inline_citation`, `structured_citation`, `citation_matches_retrieved_chunk`, `faithfulness`, `korean`, `refusal_behavior`, `fetched_at`, `posted_at`, `deadline_status`, `source_url`, `no_official_endorsement`, `no_guaranteed_outcome`, and `hostile_source_contained`. Write schema-focused tests first.</action>
  <verify>
    <automated>npm test -- data/evaluation/phase6-reference-qa.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `Phase6QaCategorySchema` contains exactly seven D-10 categories.
    - `Phase6QaCaseSchema` requires `id`, `category`, `question_ko`, `expected_retrieval`, `expected_answer`, and `required_answer_checks`.
    - Tests fail for non-Korean/empty questions, unknown categories, missing expected answer checks, and malformed freshness/deadline metadata.
    - The file imports existing `RefusalTierSchema` and deadline/citation-compatible concepts instead of hand-rolling unrelated enums.
  </acceptance_criteria>
  <done>Executors for Plan 03 can import a deterministic QA case contract without guessing fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate reference QA cases for all locked categories</name>
  <read_first>
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md` decisions D-10 through D-18 and deferred ideas
    - `.planning/phases/03-source-grounded-chat-mvp/03-06-SUMMARY.md`
    - `.planning/phases/04-personalization-and-recommendations/04-04-SUMMARY.md`
    - `scripts/evaluate-rag-mvp.ts`
    - `scripts/evaluate-personalization.ts`
  </read_first>
  <files>data/evaluation/phase6-reference-qa.ts, data/evaluation/phase6-reference-qa.test.ts</files>
  <behavior>
    - Dataset includes at least one case for CDP usage, listing/deadline discovery, success stories, guidebook/PDF, no-answer/refusal, personalization recommendation, and hostile source/prompt injection (D-10).
    - Each case has Korean `question_ko`, expected source/chunk or explicit no-answer expectations, expected refusal tier, required answer checks, and freshness/deadline expectations where applicable (D-11 through D-18).
    - Hostile-source case uses synthetic hostile metadata and asserts containment, citations, and no unsafe claims; it does not write hostile source commands into production KB artifacts.
  </behavior>
  <action>Populate `PHASE6_REFERENCE_QA_CASES` with deterministic local cases derived from existing Phase 3/4 evaluation labels and local KB source IDs. Include one no-answer case that expects transparent hard refusal/uncertainty and one hostile-source case whose hostile text is represented only as eval fixture metadata. Add tests asserting category coverage, unique IDs, Hangul questions, required checks for each category, and no official endorsement/guaranteed-outcome claims in expected safe answers.</action>
  <verify>
    <automated>npm test -- data/evaluation/phase6-reference-qa.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE6_REFERENCE_QA_CASES.length >= 7` and includes every D-10 category.
    - Every case parses through `Phase6QaCaseSchema`.
    - Listing/deadline cases require `fetched_at` and `deadline_status`; citation-bearing cases require `inline_citation` and `structured_citation`.
    - No-answer/refusal case requires `refusal_behavior`; hostile-source case requires `hostile_source_contained`, `no_official_endorsement`, and `no_guaranteed_outcome`.
    - Dataset file does not import `dotenv`, perform network calls, crawl sources, or read `.env`.
  </acceptance_criteria>
  <done>Reference QA dataset satisfies EVAL-01 and is ready for retrieval/answer evaluators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dataset→eval runner | Local fixture expectations drive pass/fail release readiness. |
| hostile fixture→answer eval | Synthetic hostile source text must remain untrusted evaluation input only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `PHASE6_REFERENCE_QA_CASES` | mitigate | Zod schema and tests enforce required categories/fields and unique IDs. |
| T-06-02-02 | Information Disclosure | QA fixtures | mitigate | Dataset contains no `.env`, keys, raw prompts, or personal data; tests inspect for secret-like tokens. |
| T-06-02-03 | Spoofing | expected safe answer characteristics | mitigate | Required checks include no official endorsement and no guaranteed outcome for all answerable cases. |
| T-06-02-04 | Tampering | hostile-source case | mitigate | Hostile content is synthetic eval fixture metadata and later eval must assert it is not obeyed or surfaced as instruction. |
</threat_model>

<verification>
Run: `npm test -- data/evaluation/phase6-reference-qa.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
EVAL-01 has a complete typed dataset and EVAL-02/EVAL-03/EVAL-06 have executable expectations to consume in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-evaluation-and-release-readiness/06-02-SUMMARY.md`
</output>
