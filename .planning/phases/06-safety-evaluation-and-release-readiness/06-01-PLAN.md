---
phase: 06-safety-evaluation-and-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/safety/disclaimer-notice.tsx
  - components/dashboard/student-dashboard.tsx
  - scripts/verify-phase6-safety.ts
  - scripts/verify-phase6-safety.test.ts
  - tests/phase6-web-smoke.spec.ts
  - package.json
autonomous: true
requirements: [SAFE-01, SAFE-02, EVAL-05]
user_setup: []
must_haves:
  truths:
    - "Users see Korean-first informational-use guidance before relying on chat answers."
    - "User-facing copy tells students to verify important details on official source pages."
    - "UI/static checks fail if official Hanyang endorsement, guaranteed outcomes, .env leakage, SSO, production crawling, saved jobs, reminders, resume tooling, or private/auth crawling claims appear."
  artifacts:
    - path: "components/safety/disclaimer-notice.tsx"
      provides: "Korean-first safety disclaimer component"
      exports: ["DisclaimerNotice"]
    - path: "scripts/verify-phase6-safety.ts"
      provides: "Static Phase 6 safety verifier"
      exports: ["verifyPhase6Safety"]
    - path: "tests/phase6-web-smoke.spec.ts"
      provides: "Desktop/mobile disclaimer visibility smoke coverage"
  key_links:
    - from: "components/dashboard/student-dashboard.tsx"
      to: "components/safety/disclaimer-notice.tsx"
      via: "render near dashboard header/chat"
      pattern: "<DisclaimerNotice"
    - from: "package.json"
      to: "scripts/verify-phase6-safety.ts"
      via: "verify:phase6-safety npm script"
      pattern: "verify:phase6-safety"
---

<objective>
Add the Korean-first safety/disclaimer surface and static safety checks required before any release-readiness evaluation.

Purpose: SAFE-01 and SAFE-02 must be visible and automatically guarded before deeper eval and checklist work relies on the UI.
Output: Disclaimer component, dashboard mount, static verifier/test, Playwright smoke assertions, and npm script.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-RESEARCH.md
@.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md
@components/dashboard/student-dashboard.tsx
@components/chat/refusal-notice-card.tsx
@components/citations/source-card.tsx
@scripts/verify-phase5-ui.ts
@scripts/verify-phase5-ui.test.ts
@tests/phase5-web-smoke.spec.ts
@package.json

<interfaces>
Use the established Phase 5 static verifier pattern: export a function returning `{ ok: boolean; failures: string[] }`, collect source files under app/components/lib/scripts/docs as needed, and print JSON in CLI mode without leaking raw secrets.

The disclaimer copy must implement D-01 through D-05 exactly: Korean-first, informational only, official source pages are authoritative, no official Hanyang endorsement, no guaranteed outcomes, and no `.env`/API key/internal config text.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and mount Korean-first disclaimer notice</name>
  <read_first>
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md` decisions D-01 through D-05 and D-25 through D-28
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md` sections `components/safety/disclaimer-notice.tsx` and `components/dashboard/student-dashboard.tsx`
    - `components/dashboard/student-dashboard.tsx`
    - `components/chat/refusal-notice-card.tsx`
  </read_first>
  <files>components/safety/disclaimer-notice.tsx, components/dashboard/student-dashboard.tsx</files>
  <behavior>
    - Renders Hangul/Korean-first text describing answers as informational guidance only (SAFE-01, D-01, D-02).
    - Tells users to verify important deadlines, application methods, eligibility, places, and other critical details on official source pages (D-02, D-04).
    - Explicitly states the product is not an official Hanyang-endorsed service and does not guarantee employment outcomes (SAFE-02, D-03, D-27).
    - Does not contain `.env`, API key, internal config, SSO, production crawling, saved jobs, reminders, resume tooling, or private/auth crawling language (D-05, D-25, D-26, D-28).
  </behavior>
  <action>Create `DisclaimerNotice` as a small presentational component using the existing card/notice class style. Mount it in `StudentDashboard` near the header or above the chat flow so it is visible on first page load on desktop and mobile. Do not modify chat, recommendation, personalization, retrieval, or provider logic; this is copy/UI only per D-04 and Phase 6 scope.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm test -- scripts/verify-phase6-safety.test.ts</automated>
    <automated>grep -rn "DisclaimerNotice" components/safety/disclaimer-notice.tsx components/dashboard/student-dashboard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `components/safety/disclaimer-notice.tsx` exports `DisclaimerNotice`.
    - `components/dashboard/student-dashboard.tsx` imports and renders `<DisclaimerNotice />` on the main dashboard path.
    - Disclaimer includes Korean text equivalent to “참고용 안내”, “공식 출처 페이지에서 확인”, “공식 한양대학교 인증 서비스가 아님”, and “취업 결과를 보장하지 않음”.
    - No source file added here contains `.env` values, API keys, SSO, production crawling, saved jobs, reminders, resume tooling, or private/auth crawling claims.
  </acceptance_criteria>
  <done>Users see the Korean-first safety disclaimer in the dashboard without any service-logic changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static Phase 6 safety verifier and npm script</name>
  <read_first>
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md` decisions D-01 through D-05 and D-25 through D-28
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md` sections `scripts/evaluate-phase6-safety.ts` and `scripts/evaluate-phase6-safety.test.ts`
    - `scripts/verify-phase5-ui.ts`
    - `scripts/verify-phase5-ui.test.ts`
    - `package.json`
  </read_first>
  <files>scripts/verify-phase6-safety.ts, scripts/verify-phase6-safety.test.ts, package.json</files>
  <behavior>
    - Positive fixture passes when disclaimer copy, official-source verification language, package script, and Phase 5 safe-link tokens are present.
    - Negative fixtures fail when disclaimer copy is absent, official endorsement/guarantee claims appear, `.env`/secret-like values appear, or v2/prohibited-scope tokens appear.
    - CLI output contains safe JSON summaries only; no raw env values, source text, prompts, or stack traces.
  </behavior>
  <action>Implement `verifyPhase6Safety(rootDir = process.cwd())` by extending the Phase 5 verifier style, not replacing it. Require the disclaimer component/dashboard mount, Korean informational-use copy, official-source verification language, package script names, and absence of prohibited claims/tokens. Add `verify:phase6-safety` to `package.json` as `tsx scripts/verify-phase6-safety.ts`. Use filtered source sweeps, not brittle comment-count gates; if using grep-like invariants in tests, ignore comments so the verifier does not self-satisfy on its own prose.</action>
  <verify>
    <automated>npm test -- scripts/verify-phase6-safety.test.ts</automated>
    <automated>npm run verify:phase6-safety</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase6-safety.ts` exports `verifyPhase6Safety` and supports CLI execution.
    - `scripts/verify-phase6-safety.test.ts` has passing positive and failing negative tests for disclaimer absence, endorsement/guarantee text, secret-like output, and prohibited Phase 6 scope tokens.
    - `package.json` includes `verify:phase6-safety` without changing existing Phase 3/4/5 scripts.
    - Default verifier performs only local file reads and does not import `dotenv`, fetch remote URLs, crawl pages, or read `.env`.
  </acceptance_criteria>
  <done>Static safety verification can fail unsafe UI/copy changes deterministically with `npm run verify:phase6-safety`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend mocked web smoke coverage for disclaimer visibility</name>
  <read_first>
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-CONTEXT.md` decisions D-01 through D-05 and D-24
    - `.planning/phases/06-safety-evaluation-and-release-readiness/06-PATTERNS.md` section `tests/phase6-web-smoke.spec.ts`
    - `tests/phase5-web-smoke.spec.ts`
    - `playwright.config.ts`
  </read_first>
  <files>tests/phase6-web-smoke.spec.ts</files>
  <behavior>
    - Desktop 1280px dashboard smoke sees the disclaimer before or alongside chat interactions.
    - Mobile 390px dashboard smoke sees the disclaimer and citation inspection remains usable.
    - Mocked API responses are used for `/api/chat`, `/api/recommendations`, and `/api/preferences`; no live provider, network, crawling, or `.env` dependency is introduced.
  </behavior>
  <action>Create `tests/phase6-web-smoke.spec.ts` by following the Phase 5 mocked route pattern. Assert Korean disclaimer phrases are visible, unsafe endorsement/guarantee claims are absent, source cards still use safe official links, and preference clear/privacy controls remain reachable. Do not add UI for operator freshness here; user-facing freshness indicators are deferred.</action>
  <verify>
    <automated>npm run qa:web -- tests/phase6-web-smoke.spec.ts</automated>
    <automated>npm run verify:phase6-safety</automated>
  </verify>
  <acceptance_criteria>
    - Playwright test file exists and uses route mocks for app APIs.
    - Desktop and mobile tests assert disclaimer visibility.
    - Tests assert no visible official endorsement or guaranteed outcome text.
    - Tests do not read `.env`, call live providers, crawl production pages, or add Phase 7/v2 flows.
  </acceptance_criteria>
  <done>Browser QA proves the safety disclaimer is visible in the existing student dashboard flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source files→static verifier | Local source text is parsed for safety invariants and prohibited tokens. |
| mocked browser route→UI | Playwright receives mocked API responses and verifies visible safety copy. |
| UI copy→student | Disclaimer wording influences whether students over-trust generated answers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | `DisclaimerNotice` / dashboard copy | mitigate | Korean copy explicitly says the service is not official Hanyang endorsement and does not guarantee employment outcomes per D-03/D-27. |
| T-06-01-02 | Information Disclosure | `verify-phase6-safety` CLI output | mitigate | Print safe JSON/failure labels only; tests assert no `.env`, API key, or secret-like value leakage per D-05/D-28. |
| T-06-01-03 | Tampering | static verifier coverage | mitigate | Positive/negative fixture tests ensure required disclaimer patterns and prohibited-token sweeps cannot silently regress. |
| T-06-01-04 | Elevation of Privilege | Phase 6 UI scope | mitigate | Static prohibited tokens reject SSO, production crawling, private/auth crawling, saved jobs, reminders, and resume tooling per D-25/D-26. |
</threat_model>

<verification>
Run: `npm run verify:phase6-safety`, `npm run qa:web -- tests/phase6-web-smoke.spec.ts`, `npm run typecheck`, and relevant Vitest tests.
</verification>

<success_criteria>
SAFE-01 and SAFE-02 are visible in UI and enforced by deterministic local static/browser checks, with no Phase 6 scope creep or secret leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-safety-evaluation-and-release-readiness/06-01-SUMMARY.md`
</output>
