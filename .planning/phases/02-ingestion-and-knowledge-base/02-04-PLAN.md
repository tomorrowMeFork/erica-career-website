---
phase: 02-ingestion-and-knowledge-base
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/ingestion/pdf/pdf-page-parser.ts
  - src/ingestion/pdf/pdf-page-parser.test.ts
  - fixtures/ingestion/cdp-student-guide-sample.pdf
autonomous: true
requirements:
  - SRC-03
  - SRC-05
must_haves:
  truths:
    - "A CDP student guide PDF sample parses into page-level normalized records with page citation anchors."
    - "PDF parser output preserves source name, official PDF URL, fetched timestamp, page number, raw text, cleaned text, content hash, and citation anchors."
    - "PDF live download remains gated by registry approval plus explicit approval record and does not imply CDP category ingestion approval."
  artifacts:
    - path: "src/ingestion/pdf/pdf-page-parser.ts"
      provides: "PDF page extraction and normalized page records"
      exports: ["extractPdfPages", "buildPdfPageRecords"]
    - path: "fixtures/ingestion/cdp-student-guide-sample.pdf"
      provides: "Small sanitized PDF fixture for page parser tests"
  key_links:
    - from: "src/ingestion/pdf/pdf-page-parser.ts"
      to: "src/ingestion/normalized-record.ts"
      via: "NormalizedRecordSchema.parse for each page record"
      pattern: "#page="
---

<objective>
Implement page-level PDF parsing for the prioritized CDP student guide PDF path.

Purpose: Deliver SRC-03 and SRC-05 for PDF ingestion while preserving D-08 page-level citations and D-06 conditional treatment of other CDP/book viewer sources.
Output: PDF page parser, sanitized PDF fixture, and tests that produce normalized citation-ready page records.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/research/seed-sources.md
@.planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
@.planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
@.planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
@.planning/phases/01-source-discovery-and-governance/discovery-notes.md
@.planning/phases/01-source-discovery-and-governance/source-registry.yaml
@.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
@.planning/phases/02-ingestion-and-knowledge-base/02-01-SUMMARY.md
@.planning/phases/02-ingestion-and-knowledge-base/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sanitized PDF fixture and parser tests</name>
  <files>fixtures/ingestion/cdp-student-guide-sample.pdf, src/ingestion/pdf/pdf-page-parser.test.ts</files>
  <read_first>
    - .planning/phases/01-source-discovery-and-governance/discovery-notes.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
    - .planning/phases/01-source-discovery-and-governance/source-registry.yaml
    - .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
    - src/ingestion/normalized-record.ts
  </read_first>
  <behavior>
    - Test 1: fixture extraction returns at least one page with `page_number: 1` and non-empty Korean or fixture text.
    - Test 2: normalized PDF record citation anchor equals the official URL plus `#page=1`.
    - Test 3: `source_id` is `cdp-student-guide-pdf` and category is `CDP 학생 매뉴얼`.
    - Test 4: parser handles empty page text by skipping or returning validation failure, not fabricating content.
    - Test 5: live/manual PDF record building rejects when `cdp-student-guide-pdf` lacks explicit approval-record evidence for `approved_manual_download`.
  </behavior>
  <action>Create a minimal sanitized PDF fixture that contains no private data and enough text to prove page extraction. If generating the fixture locally, use a deterministic script or tool and commit only the small fixture. Write tests first against `extractPdfPages` and `buildPdfPageRecords`, using the official source URL `https://cdp.hanyang.ac.kr/office/%EB%A7%A4%EB%89%B4%EC%96%BC_%ED%95%99%EC%83%9D.pdf` as metadata, not as a live network dependency.</action>
  <verify>
    <automated>npm test -- src/ingestion/pdf/pdf-page-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test fixture is small, sanitized, and stored under `fixtures/ingestion/`.
    - Tests do not download the live PDF.
    - Expected citations include `#page=` anchors.
  </acceptance_criteria>
  <done>PDF parser behavior is specified before implementation and does not rely on live network access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PDF page extraction and normalized page records</name>
  <files>src/ingestion/pdf/pdf-page-parser.ts, src/ingestion/pdf/pdf-page-parser.test.ts</files>
  <read_first>
    - .planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
    - src/ingestion/access-gate.ts
    - src/ingestion/normalized-record.ts
    - src/ingestion/chunking.ts
  </read_first>
  <action>Implement `extractPdfPages(input)` using `pdf-parse` `PDFParse` APIs from research, accepting either local fixture path/bytes for tests or an approved URL for later CLI use. Implement `buildPdfPageRecords({ source, pages, fetchedAt })` that requires both a Plan 01-approved PDF source decision and approval-record evidence for `cdp-student-guide-pdf` with `approved_manual_download`, creates one normalized record per page, sets `canonical_url` to the official PDF URL, sets citation anchors to `${canonical_url}#page=${page_number}`, computes content hashes, and validates with `NormalizedRecordSchema`. Fixture-only tests may use synthetic approved decisions; live/manual PDF ingestion must read registry plus `pre-ingestion-approval-record.md` and fail closed if CDP PDF is held/rejected/pending. Do not parse CDP categories or book viewer pages in this module.</action>
  <verify>
    <automated>npm run validate:sources && test -f .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md && npm test -- src/ingestion/pdf/pdf-page-parser.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `extractPdfPages` returns page number, raw text, cleaned text, and total page metadata when available.
    - `buildPdfPageRecords` preserves page-level citation anchors and untrusted source text marker.
    - No live download, login, storage state, or category crawling appears in tests.
  </acceptance_criteria>
  <done>CDP student guide PDF samples can become page-level normalized records with official page citations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF bytes → parser | PDF content is external untrusted input. |
| Page records → citation metadata | Page anchors can drift or be omitted if not generated deterministically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T/I | `pdf-page-parser.ts` | mitigate | Parse only approved PDF source decisions; validate every page record; mark text untrusted. |
| T-02-11 | T | citation anchors | mitigate | Generate `${canonical_url}#page=${page_number}` and test exact preservation. |
| T-02-12 | I | PDF fixtures | mitigate | Commit only small sanitized fixture; no live full corpus, cookies, credentials, or private PDFs. |
</threat_model>

<verification>
Run `npm test -- src/ingestion/pdf/pdf-page-parser.test.ts`, `npm run typecheck`, and `npm run verify:source-governance`.
</verification>

<success_criteria>
At least one PDF sample can be ingested into page-level normalized records with stable official page citation anchors, and live/manual CDP PDF ingestion remains dependent on the explicit Plan 01 approval record.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-and-knowledge-base/02-04-SUMMARY.md`.
</output>
