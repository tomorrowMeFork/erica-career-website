---
phase: 02-ingestion-and-knowledge-base
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - package-lock.json
  - src/ingestion/normalized-record.ts
  - src/ingestion/normalized-record.test.ts
  - src/ingestion/chunking.ts
  - src/ingestion/chunking.test.ts
autonomous: true
requirements:
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "Every ingested HTML listing and PDF page can be represented as a citation-ready normalized record."
    - "Every chunk preserves source identity, official URL, date/freshness fields, deadline status, and citation anchors."
    - "Source text is explicitly treated as untrusted content for later prompt-injection isolation."
  artifacts:
    - path: "src/ingestion/normalized-record.ts"
      provides: "Zod schemas and TypeScript types for normalized records, chunks, and manifests"
      exports: ["NormalizedRecordSchema", "KnowledgeChunkSchema", "IngestionRunManifestSchema"]
    - path: "src/ingestion/chunking.ts"
      provides: "Deterministic hashes, record IDs, and citation-preserving chunks"
      exports: ["sha256", "buildRecordId", "chunkNormalizedRecord"]
  key_links:
    - from: "src/ingestion/chunking.ts"
      to: "src/ingestion/normalized-record.ts"
      via: "schema-validated chunk output"
      pattern: "KnowledgeChunkSchema"
---

<objective>
Define the normalized ingestion contract and deterministic chunking foundation.

Purpose: Satisfy D-07 through D-10 so parser plans cannot lose citation, freshness, source, or deadline metadata.
Output: Runtime-valid Zod schemas, TypeScript types, hash/ID helpers, tests, and package dependencies for Phase 2 ingestion modules.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
@.planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
@.planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
@.planning/phases/02-ingestion-and-knowledge-base/02-01-SUMMARY.md

<interfaces>
Create fields required by D-07/D-08:
`source_id`, `source_name`, `source_url`, `canonical_url`, `title`, `category`, `fetched_at`, `posted_at`, `deadline_status`, `deadline_raw_text`, `raw_text`, `cleaned_text`, `content_hash`, `citation_anchors`.

Add `source_text_trust: "untrusted_source_text"` to records and chunks so Phase 3 can isolate retrieved content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime parser dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json
    - package-lock.json
    - .planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
  </read_first>
  <action>Install Phase 2 runtime ingestion libraries with `npm install cheerio pdf-parse p-limit`. Keep existing scripts intact. Do not add vector database, embedding, chat, UI, scheduler, or auth packages.</action>
  <verify>
    <automated>npm test -- src/source-governance/source-registry.schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `cheerio`, `pdf-parse`, and `p-limit`.
    - No dependency named `node-cron`, `cron`, vector DB client, or LLM SDK is added by this task.
  </acceptance_criteria>
  <done>Runtime dependencies support deterministic HTML/PDF parsing and bounded fetches without expanding scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create normalized record and manifest schemas</name>
  <files>src/ingestion/normalized-record.ts, src/ingestion/normalized-record.test.ts</files>
  <read_first>
    - src/source-governance/source-registry.schema.ts
    - src/source-governance/source-registry.schema.test.ts
    - .planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: valid HTML listing record with official detail URL passes.
    - Test 2: valid PDF page record with `page_number` and `#page=` citation anchor passes.
    - Test 3: record without citation anchors fails.
    - Test 4: chunk/manifest duplicate IDs fail validation.
    - Test 5: missing `source_text_trust: "untrusted_source_text"` fails.
  </behavior>
  <action>Create Zod schemas and inferred types for `DeadlineStatusSchema`, `CitationAnchorSchema`, `NormalizedRecordSchema`, `KnowledgeChunkSchema`, and `IngestionRunManifestSchema`. Use snake_case fields matching D-07/D-08. `deadline_status` must be `active | expired | unknown`; `content_hash` must be a 64-character SHA-256 hex string; `citation_anchors` must contain at least one official URL; PDF anchors require positive `page_number` when present. Include superRefine duplicate checks for record IDs and chunk IDs in manifest arrays.</action>
  <verify>
    <automated>npm test -- src/ingestion/normalized-record.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NormalizedRecordSchema` includes every D-07 field plus `source_text_trust`.
    - `KnowledgeChunkSchema` preserves `source_id`, `source_name`, `source_url`, `canonical_url`, `fetched_at`, `posted_at`, `deadline_status`, `deadline_raw_text`, `citation_anchors`, and `content_hash`.
    - Tests reject missing citations, malformed URLs, duplicate IDs, and trusted/source-less text.
  </acceptance_criteria>
  <done>All parser outputs can be runtime-validated before storage or retrieval experiments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic chunk IDs and citation-preserving chunks</name>
  <files>src/ingestion/chunking.ts, src/ingestion/chunking.test.ts</files>
  <read_first>
    - src/ingestion/normalized-record.ts
    - .planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `sha256("abc")` returns the standard SHA-256 hex digest.
    - Test 2: chunking a record returns stable chunk IDs across repeated runs.
    - Test 3: chunk metadata preserves citation anchors and freshness/deadline fields exactly.
    - Test 4: empty or whitespace-only cleaned text returns no chunks and no fabricated content.
  </behavior>
  <action>Create `sha256(value)`, `buildRecordId(recordInput)`, and `chunkNormalizedRecord(record, options)` using Node `crypto`. Build chunk IDs from `source_id`, record content hash, primary citation anchor or page/detail URL, and zero-based ordinal. Default chunking can be paragraph-based with max character length, but must not drop Korean text, URLs, dates, deadline fields, or citation anchors. Validate each chunk with `KnowledgeChunkSchema` before returning.</action>
  <verify>
    <automated>npm test -- src/ingestion/chunking.test.ts src/ingestion/normalized-record.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Chunk IDs are deterministic and include source identity plus content hash inputs.
    - Chunks keep `source_text_trust: "untrusted_source_text"`.
    - No embedding fields or chat-answer fields are introduced.
  </acceptance_criteria>
  <done>Records can become local KB chunks without citation drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser output → normalized schema | Untrusted external text enters local artifact format. |
| Normalized record → retrieval chunk | Metadata can be lost during transformation if not validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `normalized-record.ts` | mitigate | Require `source_text_trust: untrusted_source_text`, source IDs, official URLs, timestamps, content hashes, and citation anchors. |
| T-02-05 | S/T | `chunking.ts` | mitigate | Generate IDs from source ID + hash + anchor; validate chunks and reject missing citations. |
| T-02-06 | I | `package.json` | mitigate | Add only parser/support packages; do not add auth/session/scheduler/LLM dependencies in Phase 2 contract work. |
</threat_model>

<verification>
Run `npm test -- src/ingestion/normalized-record.test.ts src/ingestion/chunking.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
Normalized records and chunks preserve SRC-02/SRC-03/SRC-04/SRC-05 metadata and are ready for source-specific parsers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-and-knowledge-base/02-02-SUMMARY.md`.
</output>
