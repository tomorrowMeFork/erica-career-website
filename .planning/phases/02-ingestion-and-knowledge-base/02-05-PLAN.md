---
phase: 02-ingestion-and-knowledge-base
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-03
  - 02-04
files_modified:
  - .gitignore
  - package.json
  - src/ingestion/fetch-client.ts
  - src/ingestion/fetch-client.test.ts
  - src/ingestion/write-jsonl-kb.ts
  - src/ingestion/write-jsonl-kb.test.ts
  - scripts/ingest-ibus-sample.ts
  - scripts/ingest-cdp-pdf-sample.ts
  - scripts/verify-knowledge-base.ts
autonomous: true
requirements:
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "Explicitly approved sample ingestion commands can write deterministic JSONL records/chunks for one HTML source and one PDF source."
    - "Live generated KB artifacts are ignored unless deliberately sanitized; committed tests use fixtures."
    - "Verification proves source links, page/detail citation anchors, fetched timestamps, deadline status, and chunk IDs are preserved."
  artifacts:
    - path: "src/ingestion/fetch-client.ts"
      provides: "Bounded source fetch/download helper behind access gate"
      exports: ["fetchApprovedText", "fetchApprovedBytes"]
    - path: "src/ingestion/write-jsonl-kb.ts"
      provides: "Deterministic JSONL records/chunks/manifest writer"
      exports: ["writeKnowledgeBaseJsonl"]
    - path: "scripts/verify-knowledge-base.ts"
      provides: "Automated invariant checks for local KB artifacts"
  key_links:
    - from: "scripts/ingest-ibus-sample.ts"
      to: "src/ingestion/access-gate.ts"
      via: "assertCanIngestSource before fetch"
      pattern: "assertCanIngestSource"
    - from: "src/ingestion/write-jsonl-kb.ts"
      to: "src/ingestion/normalized-record.ts"
      via: "schema validation before JSONL write"
      pattern: "IngestionRunManifestSchema"
---

<objective>
Wire the gated parsers into deterministic local knowledge-base artifacts.

Purpose: Complete Phase 2 by proving one HTML and one PDF path can produce retrieval-ready JSONL records/chunks without scheduled crawling, auth bypass, citation drift, or committed live corpus data.
Output: Bounded fetch client, JSONL writer, sample CLIs, verification script, package scripts, and artifact ignore policy.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
@.planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
@.planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
@.planning/phases/01-source-discovery-and-governance/source-registry.yaml
@.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
@.planning/phases/02-ingestion-and-knowledge-base/02-01-SUMMARY.md
@.planning/phases/02-ingestion-and-knowledge-base/02-02-SUMMARY.md
@.planning/phases/02-ingestion-and-knowledge-base/02-03-SUMMARY.md
@.planning/phases/02-ingestion-and-knowledge-base/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded fetch/download client behind source gate</name>
  <files>src/ingestion/fetch-client.ts, src/ingestion/fetch-client.test.ts</files>
  <read_first>
    - src/ingestion/access-gate.ts
    - scripts/discover-cdp-seed-scope.ts
    - .planning/phases/01-source-discovery-and-governance/source-access-review.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: off-host URL for a source is rejected before `fetch` is called.
    - Test 2: pending/unapproved source decision rejects fetch.
    - Test 3: approved synthetic ibus HTML decision plus approval-record evidence calls fetch once and returns text only for HTML content type.
    - Test 4: approved synthetic CDP PDF decision plus approval-record evidence calls fetch once and returns bytes only for PDF content type.
    - Test 5: missing or non-approving `pre-ingestion-approval-record.md` rejects before `fetch` is called, even when fixture mode still works.
  </behavior>
  <action>Create `fetchApprovedText(decision, url, options)` and `fetchApprovedBytes(decision, url, options)` using built-in `fetch` plus `p-limit` concurrency 1 per host. Require an `allowed` access decision from Plan 01 and explicit source approval evidence from `pre-ingestion-approval-record.md` for the same source/method before any live fetch/download. Canonicalize URLs, reject off-host URLs, use a descriptive `User-Agent` with capstone/prototype wording if headers are set, and stop on non-2xx, throttling, or content-type mismatch. Do not store cookies, request bodies, credentials, storage state, or implement retries that resemble scheduled crawling.</action>
  <verify>
    <automated>npm test -- src/ingestion/fetch-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No fetch happens for blocked decisions or off-host URLs.
    - Client exposes a manual kill-switch option such as `AbortSignal`/timeout.
    - No scheduling primitive appears in `fetch-client.ts`.
  </acceptance_criteria>
  <done>Network access is bounded, source-gated, and safe for manual sample commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write deterministic JSONL knowledge-base artifacts</name>
  <files>src/ingestion/write-jsonl-kb.ts, src/ingestion/write-jsonl-kb.test.ts, .gitignore</files>
  <read_first>
    - src/ingestion/normalized-record.ts
    - src/ingestion/chunking.ts
    - scripts/verify-source-governance-artifacts.ts
    - .gitignore
  </read_first>
  <behavior>
    - Test 1: writing records/chunks creates `records.jsonl`, `chunks.jsonl`, and `manifest.json` with stable order.
    - Test 2: invalid record or chunk rejects before writing partial output.
    - Test 3: manifest preserves run ID, source IDs, fetched timestamps, record count, chunk count, and schema version.
    - Test 4: generated live `data/knowledge-base/` path is ignored by git policy while fixtures remain trackable.
  </behavior>
  <action>Create `writeKnowledgeBaseJsonl({ records, chunks, outputDir, manifest })` that validates with Phase 2 schemas, sorts deterministically by source/record/chunk ID, writes newline-delimited JSON with final newline, and writes a manifest JSON. Update `.gitignore` to ignore generated `data/knowledge-base/` outputs but not `fixtures/ingestion/`. Do not commit full live corpora.</action>
  <verify>
    <automated>npm test -- src/ingestion/write-jsonl-kb.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - JSONL output preserves official source URLs and citation anchors exactly.
    - Writer rejects duplicate IDs through schema/manifest validation.
    - `.gitignore` contains `data/knowledge-base/` or a narrower generated-output pattern.
  </acceptance_criteria>
  <done>Phase 2 has deterministic local KB artifact writing suitable for retrieval experiments.</done>
</task>

<task type="auto">
  <name>Task 3: Add sample ingestion and verification CLIs</name>
  <files>scripts/ingest-ibus-sample.ts, scripts/ingest-cdp-pdf-sample.ts, scripts/verify-knowledge-base.ts, package.json</files>
  <read_first>
    - src/ingestion/access-gate.ts
    - .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
    - src/ingestion/fetch-client.ts
    - src/ingestion/html/ibus-board-parser.ts
    - src/ingestion/pdf/pdf-page-parser.ts
    - src/ingestion/write-jsonl-kb.ts
    - package.json
  </read_first>
  <action>Create async CLI scripts following existing `tsx scripts/...` patterns. `ingest-ibus-sample.ts` must validate sources, assert `ibus-employment-board` is allowed for public HTML, confirm `pre-ingestion-approval-record.md` explicitly approves ibus HTML, fetch one listing/detail sample or use fixture mode when `--fixture` is passed, parse records, chunk them, and write JSONL. `ingest-cdp-pdf-sample.ts` must assert `cdp-student-guide-pdf` is allowed for manual PDF download, confirm `pre-ingestion-approval-record.md` explicitly approves CDP PDF, parse fixture/live approved PDF, chunk page records, and write JSONL. `verify-knowledge-base.ts` must read output JSONL/manifest and fail if any record/chunk lacks source URL, canonical URL, fetched timestamp, citation anchor, content hash, deadline status, or untrusted source text marker. Add scripts: `ingest:ibus:sample`, `ingest:cdp-pdf:sample`, and `verify:knowledge-base`. Fixture mode must work even while a source remains pending; live mode must fail closed until both the access gate and approval record allow it.</action>
  <verify>
    <automated>npm run validate:sources && npm run verify:source-governance && npm run ingest:ibus:sample -- --fixture --output data/knowledge-base/fixture-ibus && npm run ingest:cdp-pdf:sample -- --fixture --output data/knowledge-base/fixture-cdp-pdf && npm run verify:knowledge-base -- data/knowledge-base/fixture-ibus && npm run verify:knowledge-base -- data/knowledge-base/fixture-cdp-pdf && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Fixture-mode commands produce valid JSONL for one HTML and one PDF path.
    - Live-mode commands call `assertCanIngestSource` before any fetch/download.
    - Verification fails on missing citation anchors, missing deadline status, missing fetched timestamp, or missing source URL.
    - No command schedules repeated crawling or persists auth state.
  </acceptance_criteria>
  <done>Phase 2 can produce and verify local-first KB artifacts from gated fixture/sample ingestion paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → fetch client | External HTML/PDF bytes are untrusted and must be source-gated. |
| Parser output → filesystem | Generated corpus artifacts may contain source text and must avoid secrets/private data. |
| Fixture CLI → live CLI | Test fixture mode must not weaken live source gate behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | E/I | `fetch-client.ts` | mitigate | Reject blocked decisions/off-host URLs before fetch; low concurrency; abort/timeout kill switch. |
| T-02-14 | T | `write-jsonl-kb.ts` | mitigate | Validate records/chunks/manifests, stable sort, reject duplicates and missing citations. |
| T-02-15 | I | generated `data/knowledge-base/` | mitigate | Ignore live outputs; commit fixtures only; verification scans for missing provenance, not secrets. |
| T-02-16 | D | sample CLIs | mitigate | No schedulers, no pagination loops beyond explicit sample limits, stop on throttling/errors. |
</threat_model>

<verification>
Run the full automated command in Task 3, plus `npm test -- src/ingestion` and `npm run typecheck`.
</verification>

<success_criteria>
Fixture-mode sample ingestion proves at least one HTML source and one PDF source can become normalized, citation-ready local KB artifacts; live collection remains blocked until access evidence, explicit approval record, and registry approval permit it.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-and-knowledge-base/02-05-SUMMARY.md`.
</output>
