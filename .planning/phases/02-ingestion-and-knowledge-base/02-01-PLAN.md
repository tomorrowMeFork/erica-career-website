---
phase: 02-ingestion-and-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/access-gate.ts
  - src/ingestion/access-gate.test.ts
  - scripts/observe-pre-ingestion-gate.ts
  - .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-access-evidence.md
  - .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
  - .planning/phases/01-source-discovery-and-governance/source-registry.yaml
  - .planning/phases/01-source-discovery-and-governance/source-access-review.md
autonomous: false
requirements:
  - SRC-02
  - SRC-03
  - SRC-05
must_haves:
  truths:
    - "No Phase 2 parser or fetch command can collect a source before registry review and collection method approval are observed."
    - "CDP category and book viewer ingestion remain blocked unless safe public structure or explicit approved access evidence exists."
    - "ibus HTML and CDP student-guide PDF are prioritized as candidate sources, but still pass through the same source-gate checks."
  artifacts:
    - path: "src/ingestion/access-gate.ts"
      provides: "Registry-backed source access decisions and refusal reasons"
      exports: ["loadSourceRegistryForIngestion", "evaluateIngestionAccess", "assertCanIngestSource"]
    - path: "scripts/observe-pre-ingestion-gate.ts"
      provides: "Bounded pre-ingestion evidence command with sanitized output"
    - path: ".planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-access-evidence.md"
      provides: "Human-readable Phase 2 access/structure gate evidence"
    - path: ".planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md"
      provides: "Explicit human approval or hold decision for Phase 2 live ingestion candidates"
  key_links:
    - from: "src/ingestion/access-gate.ts"
      to: ".planning/phases/01-source-discovery-and-governance/source-registry.yaml"
      via: "SourceRegistrySchema validation"
      pattern: "SourceRegistrySchema.safeParse"
    - from: "scripts/observe-pre-ingestion-gate.ts"
      to: "src/ingestion/access-gate.ts"
      via: "evaluateIngestionAccess for every seed source"
      pattern: "evaluateIngestionAccess"
    - from: ".planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md"
      to: ".planning/phases/01-source-discovery-and-governance/source-registry.yaml"
      via: "human-approved registry status/method update for explicitly approved source IDs only"
      pattern: "approved_bounded_browser_discovery|approved_manual_download"
---

<objective>
Create the mandatory Phase 2 pre-ingestion source access gate before parser work.

Purpose: Enforce D-01 through D-06 and the deferred Phase 1 UAT gap so ingestion starts from observed safe access evidence, not assumed CDP URLs or login behavior.
Output: A typed registry gate, tests, and a sanitized pre-ingestion evidence artifact that future parser commands must consult.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
@.planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
@.planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
@.planning/phases/01-source-discovery-and-governance/source-registry.yaml
@.planning/phases/01-source-discovery-and-governance/source-access-review.md
@.planning/phases/01-source-discovery-and-governance/discovery-notes.md
@.planning/phases/01-source-discovery-and-governance/01-UAT.md

<interfaces>
Use existing NodeNext ESM `.js` import suffixes. Import registry types from `../source-governance/source-registry.schema.js`.

Required source gate statuses from Phase 1 registry:
- `review_status`: `pending | reviewed | blocked`
- `allowed_collection_method`: `none_until_review | manual_discovery_only | approved_bounded_browser_discovery | approved_manual_download`
- `scheduled_crawling_enabled`: literal `false`

Gate outcome contract to create:
```typescript
export type IngestionAccessDecision = {
  source_id: string;
  status: "allowed" | "blocked";
  collection_method: "public_html" | "manual_pdf_download" | "structure_observation_only";
  reasons: string[];
  observed_url: string;
  auth_boundary: "public" | "login_required" | "blocked" | "unknown";
  response_type: "html" | "pdf" | "viewer" | "unknown";
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement registry-backed ingestion access gate</name>
  <files>src/ingestion/access-gate.ts, src/ingestion/access-gate.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
    - src/source-governance/source-registry.schema.ts
    - src/source-governance/source-registry.schema.test.ts
  </read_first>
  <behavior>
    - Test 1: a pending source with `allowed_collection_method: none_until_review` returns `status: "blocked"` and a reason containing `not reviewed`.
    - Test 2: any source with `scheduled_crawling_enabled: true` is rejected even if other fields look approved.
    - Test 3: `cdp-career-category-discovery`, `cdp-recruit-category-discovery`, and `book-success-story-viewer` return `structure_observation_only` unless reviewed with an approved method.
    - Test 4: `ibus-employment-board` with reviewed + `approved_bounded_browser_discovery` can be allowed for `public_html`; `cdp-student-guide-pdf` with reviewed + `approved_manual_download` can be allowed for `manual_pdf_download`.
  </behavior>
  <action>Create `src/ingestion/access-gate.ts` with `loadSourceRegistryForIngestion(registryPath)`, `evaluateIngestionAccess(source, requestedMethod)`, and `assertCanIngestSource(registry, sourceId, requestedMethod)`. Use `SourceRegistrySchema.safeParse`, never redefine registry fields, and return explicit refusal reasons for D-01/D-02/D-04/D-06. Do not fetch URLs in this module. Do not add login automation, storage-state files, cron, queue, interval, or background scheduler code.</action>
  <verify>
    <automated>npm test -- src/ingestion/access-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ingestion/access-gate.ts` exports `loadSourceRegistryForIngestion`, `evaluateIngestionAccess`, and `assertCanIngestSource`.
    - Tests include blocked pending registry records and allowed synthetic reviewed records for ibus HTML and CDP PDF.
    - No test or source file contains `storageState`, `setInterval(`, `cron.schedule`, `CDP_PASSWORD =`, or literal cookies/tokens.
  </acceptance_criteria>
  <done>Ingestion access decisions are deterministic, registry-backed, and fail closed before any source collection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sanitized pre-ingestion evidence command</name>
  <files>scripts/observe-pre-ingestion-gate.ts, .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-access-evidence.md</files>
  <read_first>
    - .planning/phases/01-source-discovery-and-governance/source-registry.yaml
    - .planning/phases/01-source-discovery-and-governance/source-access-review.md
    - .planning/phases/01-source-discovery-and-governance/discovery-notes.md
    - .planning/phases/01-source-discovery-and-governance/01-UAT.md
    - scripts/discover-cdp-seed-scope.ts
    - src/ingestion/access-gate.ts
  </read_first>
  <action>Create `scripts/observe-pre-ingestion-gate.ts` that loads the registry, evaluates all six seed source IDs, prints JSON decisions, and writes/updates `.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-access-evidence.md` with observed URL structure, auth boundary, robots/ToS status, response type, public/login-gated/blocked status, and parser eligibility. Record the Phase 1 UAT CDP gap as a blocking first gate. Mark `ibus-employment-board` and `cdp-student-guide-pdf` as prioritized candidates but not automatically approved unless the registry review fields allow the requested method. The command must sanitize logs: source IDs, URLs, statuses, and reasons only; no cookies, request bodies, credentials, or storage state.</action>
  <verify>
    <automated>npm run validate:sources && npm test -- src/ingestion/access-gate.test.ts && npx tsx scripts/observe-pre-ingestion-gate.ts --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run output includes all six source IDs and blocks all currently pending records from parser collection.
    - Evidence markdown includes a `CDP category/login gap` section and states CDP category/book viewer parser work is conditional.
    - Evidence markdown states `scheduled_crawling_enabled` remains false and scheduled crawling is not implemented.
  </acceptance_criteria>
  <done>Executors have a concrete pre-ingestion gate artifact proving parser eligibility or refusal before parser plans run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Record explicit approval decision for prioritized live ingestion paths</name>
  <files>.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-access-evidence.md, .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md, .planning/phases/01-source-discovery-and-governance/source-registry.yaml, .planning/phases/01-source-discovery-and-governance/source-access-review.md</files>
  <decision>Whether the gate evidence is sufficient to approve live Phase 2 ingestion for `ibus-employment-board` and/or `cdp-student-guide-pdf`.</decision>
  <context>Per D-01/D-04/D-05, Phase 2 needs an executable path from current pending registry records to approved source-specific ingestion, but must not auto-approve sources silently. This checkpoint occurs after the gate evidence exists and before parser/live ingestion plans proceed.</context>
  <options>
    <option id="approve-ibus-html">
      <name>Approve `ibus-employment-board` for bounded public HTML sample ingestion</name>
      <criteria>Evidence confirms public board/detail URL structure, no authentication boundary, robots/ToS status recorded, source scope remains `https://ibus.hanyang.ac.kr/front/recruit/r-1`, and scheduled crawling remains false.</criteria>
      <registry_update>Set only `ibus-employment-board` to `review_status: reviewed` and `allowed_collection_method: approved_bounded_browser_discovery`; keep `scheduled_crawling_enabled: false`.</registry_update>
    </option>
    <option id="approve-cdp-pdf">
      <name>Approve `cdp-student-guide-pdf` for manual PDF download/sample ingestion</name>
      <criteria>Evidence confirms direct PDF response for the exact seed URL, page-level citation approach, auth boundary, robots/ToS status recorded, and no CDP category URL inference.</criteria>
      <registry_update>Set only `cdp-student-guide-pdf` to `review_status: reviewed` and `allowed_collection_method: approved_manual_download`; keep `scheduled_crawling_enabled: false`.</registry_update>
    </option>
    <option id="hold-or-reject">
      <name>Hold or reject one or both sources</name>
      <criteria>Evidence is missing, robots/ToS/auth boundary is not acceptable, or the user does not approve live ingestion.</criteria>
      <registry_update>Do not change the held/rejected source from `pending`/`none_until_review`; downstream live ingestion remains blocked.</registry_update>
    </option>
  </options>
  <action>After the human selects approved source IDs, create `.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md` with timestamp, approved/held source IDs, evidence-file reference, decision rationale, and the exact registry changes made. Update `source-registry.yaml` and `source-access-review.md` only for explicitly approved source IDs and only with the method listed above. Do not approve CDP category discovery or book viewer sources in this task. Do not set `scheduled_crawling_enabled` to true.</action>
  <verify>
    <automated>npm run validate:sources && npm run verify:source-governance && test -f .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md</automated>
  </verify>
  <resume-signal>Human must reply with approved source IDs (`approve-ibus-html`, `approve-cdp-pdf`, both, or `hold-or-reject`) and any rationale. Executor resumes by writing the approval record and registry/review updates exactly for approved IDs.</resume-signal>
  <done>Either ibus HTML and/or CDP PDF has explicit recorded approval plus registry method update, or the approval record clearly holds/rejects live ingestion so downstream commands fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry YAML → ingestion gate | Local planning metadata controls whether network collection is allowed. |
| Seed source URLs → observation output | External source status is untrusted and must not grant broader crawl scope. |
| Optional credentials → process env | Credentials, if ever authorized later, must not cross into logs or files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | E/I | `access-gate.ts` | mitigate | Fail closed unless `review_status: reviewed`, approved method, seed source ID, host, and `scheduled_crawling_enabled: false` are present. |
| T-02-02 | I | `observe-pre-ingestion-gate.ts` | mitigate | Sanitize output to IDs/URLs/statuses only; do not print cookies, request bodies, env values, or storage state. |
| T-02-03 | T | CDP category evidence | mitigate | Record `no_candidates_observed` and block CDP parser assumptions until observed safe structure exists. |
| T-02-17 | E/T | approval checkpoint | mitigate | Require explicit human source-ID approval before registry status/method updates; do not infer approval from evidence generation alone. |
</threat_model>

<verification>
Run `npm run validate:sources`, `npm test -- src/ingestion/access-gate.test.ts`, `npx tsx scripts/observe-pre-ingestion-gate.ts --dry-run`, and after the checkpoint `npm run verify:source-governance`. Confirm no generated file contains credential, cookie, or storage-state content and that any registry approval is backed by `pre-ingestion-approval-record.md`.
</verification>

<success_criteria>
Phase 2 has a first-wave gate that blocks unsafe CDP/category/viewer ingestion, keeps scheduled crawling disabled, and makes parser eligibility explicit for ibus HTML and CDP PDF through recorded human approval or a recorded hold/rejection.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-and-knowledge-base/02-01-SUMMARY.md`.
</output>
