---
phase: 02-ingestion-and-knowledge-base
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/ingestion/deadline-status.ts
  - src/ingestion/deadline-status.test.ts
  - src/ingestion/html/ibus-board-parser.ts
  - src/ingestion/html/ibus-board-parser.test.ts
  - fixtures/ingestion/ibus-listing.html
  - fixtures/ingestion/ibus-detail.html
autonomous: true
requirements:
  - SRC-02
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "A public ibus board listing/detail fixture parses into normalized records with official detail links and source metadata."
    - "Deadline text is classified as active, expired, or unknown without inventing dates."
    - "HTML parser output is blocked from live fetch unless Plan 01 gate plus explicit approval record permit `ibus-employment-board`."
  artifacts:
    - path: "src/ingestion/deadline-status.ts"
      provides: "Deterministic Korean deadline status extraction"
      exports: ["classifyDeadlineStatus"]
    - path: "src/ingestion/html/ibus-board-parser.ts"
      provides: "ibus HTML listing/detail parser producing NormalizedRecord values"
      exports: ["parseIbusListingPage", "parseIbusDetailPage", "buildIbusNormalizedRecords"]
  key_links:
    - from: "src/ingestion/html/ibus-board-parser.ts"
      to: "src/ingestion/normalized-record.ts"
      via: "NormalizedRecordSchema.parse"
      pattern: "NormalizedRecordSchema"
    - from: "src/ingestion/html/ibus-board-parser.ts"
      to: "src/ingestion/deadline-status.ts"
      via: "deadline status classification from title/body deadline evidence"
      pattern: "classifyDeadlineStatus"
---

<objective>
Implement fixture-first public HTML board parsing for `ibus-employment-board`.

Purpose: Deliver SRC-02, SRC-04, and SRC-05 for the clearest public HTML source while preserving the Phase 2 gate and College-of-Business scope caveat.
Output: Deadline classifier, ibus parser, sanitized fixtures, and tests producing normalized citation-ready HTML records.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/research/seed-sources.md
@.planning/phases/02-ingestion-and-knowledge-base/02-CONTEXT.md
@.planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
@.planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
@.planning/phases/01-source-discovery-and-governance/discovery-notes.md
@.planning/phases/01-source-discovery-and-governance/source-registry.yaml
@.planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
@.planning/phases/02-ingestion-and-knowledge-base/02-01-SUMMARY.md
@.planning/phases/02-ingestion-and-knowledge-base/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Korean deadline status classifier</name>
  <files>src/ingestion/deadline-status.ts, src/ingestion/deadline-status.test.ts</files>
  <read_first>
    - .planning/phases/02-ingestion-and-knowledge-base/02-RESEARCH.md
    - src/ingestion/normalized-record.ts
    - src/source-governance/source-registry.schema.test.ts
  </read_first>
  <behavior>
    - Test 1: text with `마감` returns `{ status: "expired", deadline_raw_text: "마감" }`.
    - Test 2: text with `채용시까지` or `상시` returns `active` with raw evidence.
    - Test 3: text with `D-3` returns `active`; `D-0` and `D+1` return `expired`.
    - Test 4: text without explicit deadline returns `unknown` and no fabricated deadline date.
    - Test 5: explicit Korean/date range examples preserve matched raw text even when status is `unknown` due ambiguous year.
  </behavior>
  <action>Create `classifyDeadlineStatus(text, referenceDate = new Date())` returning `{ status: "active" | "expired" | "unknown"; deadline_raw_text?: string; deadline_date?: string }`. Use deterministic string/regex parsing only. Do not use LLM extraction. Prefer `unknown` when evidence is ambiguous. Keep raw matched Korean text for later user-facing freshness labels.</action>
  <verify>
    <automated>npm test -- src/ingestion/deadline-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Deadline-free listings return `unknown`.
    - Explicit closed markers return `expired`.
    - `deadline_raw_text` is preserved for all non-unknown matches.
  </acceptance_criteria>
  <done>Freshness/deadline status is deterministic and safe for ibus parser use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse ibus listing/detail fixtures into normalized records</name>
  <files>src/ingestion/html/ibus-board-parser.ts, src/ingestion/html/ibus-board-parser.test.ts, fixtures/ingestion/ibus-listing.html, fixtures/ingestion/ibus-detail.html</files>
  <read_first>
    - .planning/phases/01-source-discovery-and-governance/discovery-notes.md
    - .planning/phases/01-source-discovery-and-governance/source-registry.yaml
    - .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md
    - .planning/phases/02-ingestion-and-knowledge-base/02-PATTERNS.md
    - src/ingestion/access-gate.ts
    - src/ingestion/normalized-record.ts
    - src/ingestion/deadline-status.ts
  </read_first>
  <behavior>
    - Test 1: listing fixture extracts at least one title and canonical detail URL like `https://ibus.hanyang.ac.kr/front/recruit/r-1/view?id=6468`.
    - Test 2: detail fixture extracts raw Korean text, cleaned text, posted date when visible, and no hit-count-only content as the title.
    - Test 3: normalized record includes `source_id: "ibus-employment-board"`, `source_name: "경상대학 취업정보 게시판"`, category `ERICA 경상대학 취업정보`, fetched timestamp, content hash, and citation anchor to the official detail URL.
    - Test 4: parser never labels ibus as campus-wide; source/category text remains faculty-specific.
    - Test 5: live/sample builder rejects `ibus-employment-board` when the access decision is blocked or when `pre-ingestion-approval-record.md` does not approve ibus HTML.
  </behavior>
  <action>Create sanitized fixture HTML based on Phase 1 observed ibus structure: board URL `https://ibus.hanyang.ac.kr/front/recruit/r-1`, Korean title `취업정보`, listing links under `/front/recruit/r-1/view?id=...`, dates, and detail body text. Implement Cheerio-based `parseIbusListingPage`, `parseIbusDetailPage`, and `buildIbusNormalizedRecords` that require the caller to pass both a Plan 01 gate-approved source decision and approval-record evidence for `ibus-employment-board` with `approved_bounded_browser_discovery`. Validate final records with `NormalizedRecordSchema`; preserve official detail URLs in `canonical_url` and `citation_anchors`. Fixture-only parser tests may use synthetic approved decisions, but any live/sample ingestion path must read the registry plus `pre-ingestion-approval-record.md` and fail closed if ibus is held/rejected/pending.</action>
  <verify>
    <automated>npm run validate:sources && test -f .planning/phases/02-ingestion-and-knowledge-base/pre-ingestion-approval-record.md && npm test -- src/ingestion/html/ibus-board-parser.test.ts src/ingestion/deadline-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Parser uses Cheerio, not regex-only HTML extraction.
    - Parser output includes title, source URL, posted date when fixture contains it, fetched timestamp, raw text, cleaned text, deadline status, and citation anchor.
    - Fixture tests do not perform live network requests.
  </acceptance_criteria>
  <done>ibus HTML source has deterministic parser coverage without bypassing access gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML fixture/live HTML → parser | Source HTML is untrusted and can contain misleading text or links. |
| Deadline text → status metadata | Incorrect parsing can mislead students about active/expired postings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | S/T | `ibus-board-parser.ts` | mitigate | Resolve links with `new URL` against approved ibus base URL and validate canonical/detail URLs. |
| T-02-08 | T | `deadline-status.ts` | mitigate | Return `unknown` for ambiguous evidence and preserve raw matched text. |
| T-02-09 | I | tests/fixtures | mitigate | Use sanitized fixtures only; no cookies, request bodies, private pages, or full live corpus committed. |
</threat_model>

<verification>
Run `npm test -- src/ingestion/html/ibus-board-parser.test.ts src/ingestion/deadline-status.test.ts`, `npm run typecheck`, and `npm run verify:source-governance`.
</verification>

<success_criteria>
At least one public HTML board fixture can be parsed into normalized, source-cited, deadline-aware records for retrieval experiments, and live/sample ibus ingestion remains dependent on the explicit Plan 01 approval record.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingestion-and-knowledge-base/02-03-SUMMARY.md`.
</output>
