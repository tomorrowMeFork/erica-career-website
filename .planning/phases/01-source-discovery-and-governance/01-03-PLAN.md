---
phase: 01-source-discovery-and-governance
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/01-source-discovery-and-governance/discovery-notes.md
  - scripts/discover-cdp-seed-scope.ts
autonomous: true
requirements: [SRC-01, SAFE-05]
user_setup:
  - service: cdp.hanyang.ac.kr
    why: "Only if approved seed-scope CDP discovery requires login during execution"
    env_vars:
      - name: CDP_USERNAME
        source: "Local .env; do not commit"
      - name: CDP_PASSWORD
        source: "Local .env; do not commit"
must_haves:
  truths:
    - "SRC-01: Discovery notes record observed CDP category paths or explicit login/no-candidate evidence, PDF/viewer feasibility, and ibus board assumptions for downstream source registration."
    - "SAFE-05: Discovery remains bounded and one-shot; it does not create cron, queue, interval, or scheduled crawling behavior."
    - "D-04/D-05/D-06: If browser or authenticated discovery is needed, it is seed-scope only and reads credentials from environment without logging or storing them."
  artifacts:
    - path: ".planning/phases/01-source-discovery-and-governance/discovery-notes.md"
      provides: "Discovery evidence and unresolved source-access notes"
      contains: "## CDP Category Discovery"
    - path: "scripts/discover-cdp-seed-scope.ts"
      provides: "One-shot bounded CDP link enumeration helper"
      contains: "SEED_SCOPE_HOSTS"
  key_links:
    - from: "scripts/discover-cdp-seed-scope.ts"
      to: ".planning/phases/01-source-discovery-and-governance/discovery-notes.md"
      via: "executor runs npm run discover:cdp and records JSON output or access status"
      pattern: "scheduled_crawling_enabled"
---

<objective>
Create bounded source-discovery notes and a one-shot CDP seed-scope helper.

Purpose: Phase 1 must document CDP category discovery, PDF/viewer feasibility, and ibus board behavior without turning discovery into ingestion or scheduled crawling. The helper gives executors a controlled way to enumerate seed-scope CDP links if static fetch remains insufficient.

Output: Discovery notes plus a seed-scope-only script that is safe by default, does not persist credentials, does not schedule repeated collection, and is executed once to capture CDP candidate URL evidence or access status.
</objective>

<execution_context>
@/Users/wantap/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/wantap/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@sources.txt
@.planning/research/seed-sources.md
@.planning/phases/01-source-discovery-and-governance/01-CONTEXT.md
@.planning/phases/01-source-discovery-and-governance/01-RESEARCH.md
@src/source-governance/source-registry.schema.ts

<interfaces>
Discovery constraints:
- Seed scope only: `https://cdp.hanyang.ac.kr/`, `https://book.hanyang.ac.kr/Viewer/YKCF2I67RO4B`, `https://cdp.hanyang.ac.kr/office/%EB%A7%A4%EB%89%B4%EC%96%BC_%ED%95%99%EC%83%9D.pdf`, `https://ibus.hanyang.ac.kr/front/recruit/r-1`.
- Do not ingest all posts, PDFs, or viewer pages in Phase 1.
- Do not create cron jobs, intervals, queues, daemons, background schedulers, or CI schedules.
- If login appears required, read `CDP_USERNAME` and `CDP_PASSWORD` from environment only; never print, persist, or commit credentials/storage state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bounded CDP seed-scope discovery helper</name>
  <files>scripts/discover-cdp-seed-scope.ts</files>
  <read_first>.planning/phases/01-source-discovery-and-governance/01-CONTEXT.md; .planning/phases/01-source-discovery-and-governance/01-RESEARCH.md; sources.txt; package.json</read_first>
  <action>Create `scripts/discover-cdp-seed-scope.ts` as a one-shot helper for CDP category URL enumeration. Use the Playwright dev dependency installed by Plan 01. Define `SEED_SCOPE_HOSTS = ["cdp.hanyang.ac.kr"]` and reject navigation to any other host. Navigate only to `https://cdp.hanyang.ac.kr/`. Extract links whose text or URL includes Korean category hints `취업정보`, `채용정보`, `recruit`, or `job`; print JSON with `url`, `text`, and `reason`. Use low concurrency by design: one browser page, no parallel navigation, no auto-pagination. Do not call `setInterval`, `cron`, queues, or background scheduling. If `CDP_USERNAME`/`CDP_PASSWORD` exist, keep them in memory only and do not print them; if login is encountered, print `login_required_env_credentials` without values and stop unless explicit login selectors are later added by a human. Include `--dry-run` mode that prints the seed scope, host allowlist, and forbidden scheduling status without navigating so type/safety checks can run without network. Follow the security/secret analog in `01-PATTERNS.md` lines 72-81 and seed-source caveat analog in lines 198-234.</action>
  <verify>
    <automated>npm run typecheck && npm run discover:cdp -- --dry-run && npm run verify:source-governance</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/discover-cdp-seed-scope.ts` contains `SEED_SCOPE_HOSTS` and `cdp.hanyang.ac.kr`.
    - `scripts/discover-cdp-seed-scope.ts` contains `CDP_USERNAME` and `CDP_PASSWORD` but does not contain any literal credential value.
    - `npm run verify:source-governance` fails if `scripts/discover-cdp-seed-scope.ts` contains executable `setInterval(`, `cron.schedule`, or credential literals.
    - `scripts/discover-cdp-seed-scope.ts` contains `login_required_env_credentials`.
  </acceptance_criteria>
  <done>Executor has a bounded, non-scheduled browser discovery helper for approved seed-scope CDP category enumeration.</done>
</task>

<task type="auto">
  <name>Task 2: Run bounded discovery and write notes for CDP, PDF/viewer, and ibus sources</name>
  <files>.planning/phases/01-source-discovery-and-governance/discovery-notes.md</files>
  <read_first>.planning/phases/01-source-discovery-and-governance/01-RESEARCH.md; .planning/research/seed-sources.md; sources.txt; scripts/discover-cdp-seed-scope.ts</read_first>
  <action>Run `npm run discover:cdp` once. If it returns observed candidate URLs, record each URL, link text, and reason under `## CDP Category Discovery` or `## CDP Recruitment Category Discovery`. If it returns `login_required_env_credentials`, record that exact status and do not attempt bypass or credential logging. If it returns no category candidates, record `no_candidates_observed` with the command timestamp and keep next action as manual inspection within seed scope. Then create `discovery-notes.md` with sections `## CDP Category Discovery`, `## CDP Recruitment Category Discovery`, `## CDP Student Guide PDF`, `## Book Success Story Viewer`, `## ibus Employment Board`, `## Open Questions`, and `## Non-Ingestion Boundary`. Copy the research facts: CDP static markdown fetch failed and may need browser/manual discovery; CDP PDF is a 52-page PDF requiring later page-level citation; book viewer title is `[E 커리어개발팀] 2024 EBS 취업성공후기_한양인의 취업지식기부`; ibus exposes `취업정보`, 12 listings per page, detail links like `/front/recruit/r-1/view?id=6468`, and pagination to page 163. Include an explicit statement: `scheduled_crawling_enabled: false for all Phase 1 discovery outputs`. Do not fabricate URLs not observed. Follow the discovery-notes analog in `01-PATTERNS.md` lines 198-234.</action>
  <verify>
    <automated>npm run typecheck && test -f .planning/phases/01-source-discovery-and-governance/discovery-notes.md && npm run verify:source-governance</automated>
  </verify>
  <acceptance_criteria>
    - `discovery-notes.md` contains `## CDP Category Discovery`.
    - `discovery-notes.md` contains one of: `login_required_env_credentials`, `no_candidates_observed`, or at least one observed `https://cdp.hanyang.ac.kr/` candidate URL.
    - `discovery-notes.md` contains `scheduled_crawling_enabled: false for all Phase 1 discovery outputs`.
    - `discovery-notes.md` contains `52-page PDF`.
    - `discovery-notes.md` contains `/front/recruit/r-1/view?id=6468`.
    - `discovery-notes.md` contains `do not fabricate URLs not observed`.
  </acceptance_criteria>
  <done>Discovery notes preserve known evidence, unknowns, and explicit non-ingestion boundaries for downstream planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-discovered links → discovery notes | External page content is untrusted and must not expand scope beyond seed hosts. |
| Local environment credentials → Playwright helper | Secrets may exist locally for approved access but must not be printed or persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Elevation of Privilege | CDP discovery helper | mitigate | Hard-code seed-scope host allowlist and reject off-host navigation per D-01/D-02/D-04. |
| T-01-10 | Information Disclosure | CDP credentials | mitigate | Read `CDP_USERNAME`/`CDP_PASSWORD` from environment only; never log values or persist storage state per D-05/D-06. |
| T-01-11 | Denial of Service | Browser discovery | mitigate | One-shot single-page execution only; no auto-pagination, cron, queue, interval, or schedule; preserve moderate posture per D-10/D-11. |
| T-01-12 | Tampering | External link text | mitigate | Treat discovered URLs as candidates in notes; do not ingest content or mark as approved without checklist review. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm run discover:cdp -- --dry-run`, and `npm run verify:source-governance`; verify no scheduling primitives or credential values were introduced.
</verification>

<success_criteria>
- CDP, PDF/viewer, and ibus discovery notes exist with evidence and open questions.
- Any browser discovery helper is seed-scope-only, one-shot, and credential-safe.
- No scheduled crawling or ingestion is created in Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-discovery-and-governance/01-03-SUMMARY.md`
</output>
